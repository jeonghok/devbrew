# 검토를 마친 red. <파일명> <정규화된 실패 출력 sha256>
#
# 양방향이다: 미등재·해시 불일치 실패는 RED, 등재됐는데 GREEN이 된 항목도 RED.
# 목록은 줄어들기만 한다 — 새 red를 여기 넣는 것은 검토를 마쳤다는 선언이다.
#
# 정규화: <REPO>/<TMP> 경로 치환 + 숫자 → N + 실패 줄(FAIL|✗)만.
# 이유: 등재 대상 하나의 출력이 quality-pipeline/SKILL.md의 grep 카운트를 담아,
#       그 파일을 고칠 때마다 원시 해시가 바뀐다. 어떤 assert가 실패하는지(문구)는
#       남으므로 같은 파일이 **다른 이유로** 실패하기 시작하면 해시가 바뀐다.
#
# 2026-08-22 ~ 2026-09-03 은 **등재 0건**이었다. 빈 원장은 고장이 아니라 규약
# ("목록은 줄어들기만 한다")의 도달점이며, 메커니즘은 그대로 살아 있다 — 새 red를
# 여기 등재하지 않으면 test_codex_backward_compat.sh가 `미등재`로 RED를 낸다.
#
# 마지막 두 항목이 빠진 경위 (기준선 RED 4건 해소 사이클):
#  - consent-marker 판: 그 **테스트 자체가 삭제**됐다. 지키던 메커니즘(SKILL.md의
#    cost-consent 마커)이 `753c9e2`의 v2.0.0 재작성에서 사라져 테스트만 남아
#    RED였다. 소비자 루프는 실재하는 파일만 도는지라 등재만 남겨도 조용했겠지만,
#    대상이 없어진 등재는 같은 커밋에서 지운다.
#  - security-reviewer kill switch 판: 그 테스트가 **GREEN이 됐다**. 약속만 있고
#    집행이 없던 kill switch를 실제로 dispatch 지점에 구현했기 때문이다. 등재를
#    남겨두면 위 양방향 래칫이 `stale 등재`로 RED를 낸다(실측 확인).
#
# test_synthesize_disposition.sh 판이 빠진 경위 (2026-09-04 등재 → 2026-09-04
# 지움, adjudication-topology PR2 Task 8→10 수정 라운드 1, 위 사이클과 별개):
# Task 8 이 원장 배선을 마친 뒤 여섯 단언 중 다섯이 render() 소비를 기다리는
# 설계상 RED로 등재됐다. Task 10 이 render()를 배선했으나 마지막 한 단언
# ("배관 손실 [1-9]")의 grep 패턴이 실제 렌더 서식("**배관 손실:** N" — 라벨과
# 콜론+볼드가 숫자 앞에 온다)과 애초에 맞을 수 없었다(계획 문서
# 2026-09-03-adjudication-topology.md 작성 단계부터의 불일치, distillation
# 오탈자가 아님). 고칠 것은 렌더 서식이 아니라 단언이었다 — line1(`**처분:**
# 수용 N · …`)과 대칭으로 line2 도 굵은 도입 라벨 + 맨 항목 구조이므로, 단언을
# 라벨 전체(`**배관 손실:**`)까지 고정하도록 넓혔다(수정 라운드 1). 6/6
# GREEN이 되어 등재를 지운다 — 위 양방향 래칫이 그것을 요구한다.
#
# 2026-09-04 현재 **등재 0건**이다.
