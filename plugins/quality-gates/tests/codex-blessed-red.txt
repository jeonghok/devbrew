# 검토를 마친 red. <파일명> <정규화된 실패 출력 sha256>
#
# 양방향이다: 미등재·해시 불일치 실패는 RED, 등재됐는데 GREEN이 된 항목도 RED.
# 목록은 줄어들기만 한다 — 새 red를 여기 넣는 것은 검토를 마쳤다는 선언이다.
#
# 정규화: <REPO>/<TMP> 경로 치환 + 숫자 → N + 실패 줄(FAIL|✗)만.
# 이유: 등재 대상 하나의 출력이 quality-pipeline/SKILL.md의 grep 카운트를 담아,
#       그 파일을 고칠 때마다 원시 해시가 바뀐다. 어떤 assert가 실패하는지(문구)는
#       남으므로 같은 파일이 **다른 이유로** 실패하기 시작하면 해시가 바뀐다.
#
# 이 둘은 codex와 무관한 pre-existing red다 (설계 §8.1 범위 밖 — 고치지 말 것).
#
# 2026-08-17 devbrew-weight-reduction Task 14: 두 파일이 자체 판정 헬퍼를 지우고
# shared/tests/assert.sh 정본을 source하며 실패 줄 접두가 `FAIL: `/`✗ FAIL: `에서
# `  ✗ `로 바뀌었다(브리프 실측: 접두를 grep하는 소비자 0건 — 출력 형식 변경은
# 안전). 두 실패의 **문구·원인은 불변**(같은 pre-existing red) — 그래서 해시만
# 갱신한다. 재현: `bash tests/<file>.sh` 로 실패 줄이 기존과 동일한 메시지인지
# 먼저 확인한 뒤에만 아래 해시를 바꿀 것.
test_consent_marker_write_failure.sh 9570188161db7b4e49ade31f1fd596d80ea940237a64589233e25a2bc3c757de
test_security_reviewer_kill_switch.sh 09e3e6d29c124351a9fc175f930e906d0ddbd95565f36d5053291413086e43e9
