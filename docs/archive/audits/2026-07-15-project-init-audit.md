# project-init 읽기전용 감사 — 2026-07-15

## 발견
### [CRITICAL] Trunk-based legacy release 브랜치를 현재 main에서 잘못 생성한다 (CX-2) (심층검증 통과)
- `plugins/project-init/templates/trunk-based/branch-strategy.md:86` — 이미 출시된 옛 version (예 v1.x)에 hotfix를 backport해야 할 때만
- `plugins/project-init/templates/trunk-based/branch-strategy.md:92` — git checkout main
- `plugins/project-init/templates/trunk-based/branch-strategy.md:94` — git checkout -b release/v1.x
- 피해: 현재 main이 v2 코드인 프로젝트에서 이미 출시된 v1.x에 v1.2.5 hotfix를 하려고 템플릿을 따르면 release/v1.x가 v2 HEAD에서 생성된다. 이후 v1 fix를 cherry-pick하고 v1.2.5 태그를 찍으면 v2 계보의 생성물이 v1 패치로 배포될 수 있다.
- 권고: legacy release 브랜치는 마지막 해당 계열 release 태그에서 생성하도록 명령을 바꾼다. 이미 브랜치가 있으면 새로 만들지 말고 checkout하도록 분기하고, 템플릿 계약 테스트에 branch base 검증을 추가한다.
- 반대근거: 최초 release branch cut 시점의 main이 아직 정확히 v1 계보라면 현재 절차도 맞고, 이후에는 기존 release/v1.x를 재사용할 수 있다. 그러나 템플릿은 그 전제나 재사용 분기를 명시하지 않는다.
- 레퍼런스 격차: https://trunkbaseddevelopment.com/branch-for-release/

### [HIGH] S4(i) rewrites a divergent CLAUDE.md to a one-line pointer without migrating its content — deterministic user-content loss that contradicts the plugin's own "preserve in all states" invariant (A6-1) (심층검증 통과)
- `plugins/project-init/commands/project-init.md:165` — **S4 (AGENTS exists, CLAUDE divergent or absent)** | 존재 | 없음 또는 divergent content | 사용자에게 advisory + 두 옵션 — (i) CLAUDE.md를 `@AGENTS.md` 한 줄로 *재작성* (AGENTS.md unchanged), (ii) abort. 승인 시 (i) 수행 + S3 action.
- `plugins/project-init/commands/project-init.md:167` — 비-관리 컨텐츠 (다른 헤딩, 단락, 코드 블록)는 모든 state에서 보존. 유일한 예외는 4c S2a (d)의 H1 재제목 — 파일명을 지칭하는 제목은 이전 후 대상 파일을 잘못 가리키므로 보존 대상이 아니다.
- 피해: A user who has an AGENTS.md (canonical) plus a separately hand-written CLAUDE.md containing Claude-specific instructions not present in AGENTS.md is in state S4. Running /project-init shows an advisory with two options; choosing option (i) rewrites CLAUDE.md to the single line `@AGENTS.md` while leaving AGENTS.md unchanged. The unique CLAUDE.md content is neither preserved in CLAUDE.md nor migrated into AGENTS.md — it is discarded. Because `@AGENTS.md` imports only AGENTS.md, that content also vanishes from the model's loaded context. There is no filesystem backup; recovery is possible only via git, which fails if the CLAUDE.md was uncommitted. Crucially, the tool's own spec (line 167) promises non-managed content is preserved in ALL states with a single named exception (S2a-d), so a user reading that promise reasonably believes option (i) is a safe reformat — the consent is obtained under a false safety guarantee.
- 권고: Make the S4(i) action mirror the content-preserving S2a path: extract non-managed content from the divergent CLAUDE.md and merge it into AGENTS.md BEFORE rewriting CLAUDE.md to the pointer (so AGENTS.md is not left 'unchanged' when CLAUDE.md holds unique content). Alternatively, if S4(i) must remain a discard, (a) update the line-167 invariant to name S4(i) as a second explicit exception (so the spec stops lying), (b) require the S4 advisory to state verbatim that divergent CLAUDE.md content will be permanently discarded and recommend commit/backup first, and (c) add a contract-test lock in test_command_contract.py (which today locks only the S2a/AC21 prompt, not any S4 disclosure).
- 반대근거: S4 is an uncommon state (most repos have either CLAUDE.md or AGENTS.md, not a divergent pair), the destruction is gated behind an explicit two-option user choice (not silent), and project-init's entire domain is git repositories — so a committed CLAUDE.md is recoverable via `git checkout`. One could argue 'divergent CLAUDE.md when AGENTS.md is canonical' is by definition stale content the user intends to abandon, making the discard the desired behavior; and the AGENTS.md-canonical design (D5-confirmed correct) means consolidating on one file is the point. A reviewer could therefore rate this MEDIUM (gated + git-recoverable) rather than HIGH.
- 레퍼런스 격차: The plugin's OWN S2a path is the internal reference for how a canonical-switch should behave: 4c S2a migrates non-managed content into AGENTS.md before replacing CLAUDE.md with the pointer (line 163). S4(i) omits that migrate step while the line-167 invariant still asserts universal preservation. No external corpus precedent is required — the spec is internally self-contradicting, and S2a shows the correct pattern the S4 row fails to follow.

## 배정된 열린 질문 (OQ)
### OQ1
- 출처: claude
  - 좌:
    - `plugins/project-init/CHANGELOG.md:14` — None: **S2a migration이 `AGENTS.md`를 `# CLAUDE.md`로 잘못 제목 짓던 문제** — ... 산출된 `AGENTS.md`가 자기 자신이 아닌 원본 파일명을 제목으로 달았다.
    - `plugins/project-init/CHANGELOG.md:15` — None: 발견 경위: devbrew context-slimming WS5의 sandbox 재실행 — 감량된 repo 사본에서 `/project-init`을 S2a 승인 경로로 실제 수행해 산출물을 검사했다.
    - `plugins/project-init/hooks/tests/test_command_contract.py:4` — None: a test cannot exercise its behaviour. What it *can* lock is the existence of the contract the model is meant to follow.
    - `plugins/project-init/commands/project-init.md:165` — None: **S4 (AGENTS exists, CLAUDE divergent or absent)** | 존재 | 없음 또는 divergent content | 사용자에게 advisory + 두 옵션 — (i) CLAUDE.md를 `@AGENTS.md` 한 줄로 *재작성* (AGENTS.md unchanged)
  - 우:
    - `docs/philosophy/devbrew-harness-philosophy.md:77` — None: (제거 방향) 편의·라우팅·NL-의도처럼 구조적 escape hatch가 이미 있는 영역엔 결정론 가드를 쌓지 말고 모델을 신뢰한다.
    - `docs/philosophy/devbrew-harness-philosophy.md:78` — None: (부과 방향) 결정론은 보안/정확성 게이트(fail-closed)라는 load-bearing 지점에만 정확히 부과한다.
    - `plugins/project-init/hooks/hooks.json:4` — None: "PostToolUse": [ { "matcher": "Bash", ... "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/post-tool-use.py" ... }, { "matcher": "Write|Edit|MultiEdit", ... "docs-lint.py" ... } ]
    - `docs/philosophy/devbrew-harness-philosophy.md:96` — None: Subagent spray — 선언 없는 fan-out ≥5. single-agent가 default다 (P22).
  - 스틸맨 조건: c
  - 근거: 얇음은 결함이 아니라 **적합 설계(fit design)**로 판정한다. 지배 규범 P8(Determinism Economy, philosophy:74-80)은 결정론 장치를 '모델 신뢰가 불충분하고 오류 비용이 높은 load-bearing 지점'에만 부과하고(부과 방향), '편의·라우팅·NL-의도처럼 구조적 escape hatch가 있는 영역엔 결정론 가드를 쌓지 말고 모델을 신뢰'(제거 방향, philosophy:77)하라고 대칭으로 명한다. project-init은 정확히 이 분할을 이미 구현한다: load-bearing 결정론 표면(브랜치/커밋 검증, 문서-컨벤션 린트)은 *실제 Python 코드*(post-tool-use.py, docs-lint.py; 95/95 테스트)로 존재하고, 모델-판단 표면(인터뷰, 문서 생성)만 산문이다. 생성 로직에 scripts/skills/agents를 추가하는 것은 P8의 제거 방향과 memory 선례('harness lightness — trust the model', 'harness is means not end')를 위반하는 과잉-결정론이다. LD6 입증책임: steelman 조건 중 현재 충족된 것이 없다 — (a) command 231줄 << 500(불충족); (b) 산문-로직 버그는 v1.7.2 단 1건, 아직 '반복' 패턴 아님(불충족); (d) 판정 로직을 재사용할 타 플러그인 소비자 부재(불충족). (c)=되돌릴 수 없는 파괴에 PreToolUse 게이트가 필요한 사례는 축6 OQ4에 의존 → pending. 재현 가능한 *미수정* 실패 모드도, 충족된 steelman도 없으므로 구조 변경을 권고하지 않는다. 결론: 축6이 (c)-급 파괴 사례를 확증하지 않는 한 얇음은 적합 설계다.
- 출처: codex
  - 좌:
    - `plugins/project-init/commands/project-init.md:165` — None: | **S4 (AGENTS exists, CLAUDE divergent or absent)** | 존재 | 없음 또는 divergent content | 사용자에게 advisory + 두 옵션 — (i) CLAUDE.md를 `@AGENTS.md` 한 줄로 *재작성* (AGENTS.md unchanged), (ii) abort. 승인 시 (i) 수행 + S3 action. |
    - `plugins/project-init/CHANGELOG.md:14` — None: - **S2a migration이 `AGENTS.md`를 `# CLAUDE.md`로 잘못 제목 짓던 문제**
    - `plugins/project-init/hooks/tests/test_command_contract.py:3` — None: `/project-init` is a model-followed markdown instruction, not executable code, so
  - 우:
    - `/Users/jeonghokim/.claude/plugins/cache/claude-plugins-official/plugin-dev/unknown/skills/command-development/SKILL.md:9` — None: Both are loaded identically — the only difference is file layout.
    - `/Users/jeonghokim/.claude/plugins/cache/claude-plugins-official/skill-creator/unknown/skills/skill-creator/SKILL.md:96` — None: Keep SKILL.md under 500 lines; if you're approaching this limit, add an additional layer of hierarchy
    - `CLAUDE.md:67` — None: - **Trivia ceremony** — 한 문장 diff에 full pipeline 실행 (Anthropic *Best Practices*).
    - `plugins/project-init/commands/project-init.md:231` — None: > 간결한 git 작업을 위해 `/commit` 또는 `/commit-push-pr` (commit-commands 플러그인) 사용.
  - 스틸맨 조건: c
  - 근거: 500줄 기준은 SKILL.md 규범이고 현재 command는 231줄이라 조건 (a)가 아니다. command와 skill은 동일하게 로드되므로 단순 포맷 이동의 실행상 이득도 없다. 반면 기존 AGENTS-only workflow를 감지하지 못하는 실제 파괴 경로와 과거 산문 state bug가 존재하고, 현재 write에는 백업이 없으므로 steelman (c)가 충족된다. 추출 범위는 인터뷰 전체가 아니라 write inventory·backup·atomic commit으로 제한해야 중복 상태기계와 drift 비용을 줄일 수 있다.

### OQ2
- 출처: claude
  - 답: Do NOT promote branch/commit validation to a BLOCKING PreToolUse. Keep the current PostToolUse advisory as the default. A genuine middle ground exists — a PreToolUse hook that emits a WARNING ONLY (never permissionDecision deny/ask) before `git checkout -b`/`switch -c`/`branch` runs — and it would modestly improve the branch-naming UX by warning before creation instead of suggesting a post-hoc rename. But that middle ground is optional polish, not a defect to fix: it adds a second hook on the Bash PreToolUse hot path and can fire noise on commands that ultimately fail, for a benefit (avoiding one recoverable `git branch -m`) that the existing advisory already makes cheap.
  - 근거: The post-hoc limitation is REAL and verified: PostToolUse fires after the tool executes, so by the time validate_branch runs the badly-named branch already exists and the hook can only offer `git branch -m <prefix>/{name_part}` (post-tool-use.py:128) — a rename, not a prevention. Likewise a commit is already made. That fact is what motivates the OQ. However, three lines of evidence make a BLOCKING promotion the wrong call: (1) branch naming is a recoverable STYLE rule, not a safety/correctness gate — a wrong name costs one rename, versus the destructive/boundary operations that legitimately warrant PreToolUse deny/ask in the precedent corpus (gstack freeze denies out-of-boundary writes, careful ASKS at check-careful.sh:109; ECC path-safety fails closed on trust-root escape). (2) The maintainers already made exactly this call deliberately: CHANGELOG.md:40 records that runtime base-branch/merge enforcement (F4/F5) was 'harness lightness — trust the model'로 명시 defer, and README.md:83 documents the hook as advisory/non-blocking on purpose. (3) devbrew's own standing lesson (feedback_harness_lightness_trust_model) reserves deterministic guards for load-bearing security/accuracy gates, not convention nudges — and sibling precedent (oh-my-claudecode merge-readiness: 'v1 is advisory does not block') keeps convention checks advisory. So: the middle ground is real but not compelled; promoting to deny would over-enforce a style rule against the plugin's own declared design and philosophy. Recommendation: keep PostToolUse advisory; only if the team specifically wants to kill the rename-dance should they add a PreToolUse WARNING-ONLY variant, and even then never deny/ask.
- 출처: codex
  - 답: 브랜치·커밋 검증 전체를 hard-blocking PreToolUse로 승격하지 않는다. 현재 PostToolUse 경고를 유지하되, commit+push 같은 원격 side effect가 한 호출에 결합된 경우에는 선택적 PreToolUse ask를 제공하는 중간지대가 적합하다.
    - `plugins/project-init/hooks/hooks.json:4` — "PostToolUse": [
    - `plugins/project-init/hooks/post-tool-use.py:208` — warnings = [w for w in (validate_branch(command), validate_commit(command)) if w]
    - `plugins/project-init/hooks/post-tool-use.py:211` — print(json.dumps({"systemMessage": "\n\n".join(warnings)}))
    - `plugins/project-init/hooks/post-tool-use.py:215` — sys.exit(0)
    - `plugins/project-init/hooks/post-tool-use.py:178` — def kill_switch_active():
    - `/Users/jeonghokim/Downloads/reference/gstack/careful/bin/check-careful.sh:109` — printf '{"permissionDecision":"ask","message":"[careful] %s"}\n' "$WARN_ESCAPED"
    - `/Users/jeonghokim/Downloads/reference/gstack/freeze/bin/check-freeze.sh:77` — printf '{"permissionDecision":"deny","message":"[freeze] Blocked: %s is outside the freeze boundary (%s). Only edits within the frozen directory are allowed."}\n' "$FILE_PATH" "$FREEZE_DIR"
  - 근거: 현재 hook은 systemMessage만 반환하고 항상 exit 0이라 실제 차단 능력이 없다. local branch 생성과 commit은 대체로 복구 가능하고 release 예외도 있어 전면 deny는 오탐 비용이 크다. 반면 잘못된 commit 뒤 push까지 한 Bash 호출이면 사후 경고가 원격 결과를 막지 못한다. permissionDecision ask 선례는 사람에게 최종 선택을 남기는 비차단·비자동승인 중간지대다. 두 kill switch는 post-tool-use.py:178-183 및 docs-lint.py:31-40에서 main 진입 전에 실제로 존중된다. 기존 테스트는 지원된 checkout -b·switch -c·-m과 kill switch를 검증하지만 Git 전체 문법이나 사전 차단을 증명하지는 않는다.

### OQ3
- 출처: claude
  - 답: Phase 0's manifest/directory scan IS mechanically redundant with the built-in /init's codebase analysis, and the newer interactive /init (CLAUDE_CODE_NEW_INIT=1, documented in official memory docs) overlaps even further — it 'explores your codebase with a subagent, fills in gaps via follow-up questions, and presents a reviewable proposal,' which parallels project-init's Phase 0 (scan) + Phase 1 (AskUserQuestion elicitation). BUT project-init should NOT simply delegate to /init and it retains independent value, for three concrete reasons: (1) DIRECTION — /init is CLAUDE.md-canonical (it 'reads [AGENTS.md] and incorporates the relevant parts into the generated CLAUDE.md'), whereas project-init emits AGENTS.md-canonical + CLAUDE.md=@AGENTS.md, the officially-recommended multi-vendor direction (D5 confirmed). Delegating to /init would invert the canonical file. (2) SCOPE — Phase 0's scan is a means to charter elicitation (each hit gets a [감지됨] label confirmed at Phase 1 ④), not general memory-file authoring. (3) DELIVERABLE — /init 'discovers' conventions; it does not elicit a durable Project Charter (vision/non-goals/success/personas in docs/project/charter.md) behind a Law 1 completeness gate, nor does it generate the 3 branching-strategy templates + docs/git-workflow/ + branch/commit enforcement hooks. Conclusion: the Phase 0 scan overlaps but is a tolerable, partial duplication serving a distinct downstream purpose — it does not remove project-init's reason to exist. This is an open question resolved in project-init's favor, NOT a gap (LD6: no reproducible failure, no steelman condition met). A docs nicety worth considering: README/command do not mention /init at all, so a user who already ran /init isn't told Phase 0 re-scans; acknowledging /init would set expectations, but that is cosmetic, not load-bearing.
  - 근거: Overlap on the tech-stack scan is real but confined to Phase 0; project-init's AGENTS.md-canonical direction, charter-scoped elicitation, and git-workflow+charter deliverables are not provided by /init. Independent value holds; not a gap.
- 출처: codex
  - 답: Phase 0은 내장 /init과 부분 중복하지만 대체 가능한 동일 기능은 아니다.
    - `plugins/project-init/commands/project-init.md:89` — `Glob`/`Read`로 repo를 스캔해 tech-stack 후보를 자동 생성한다. manifest → stack 라벨 매핑:
    - `plugins/project-init/commands/project-init.md:113` — `AskUserQuestion`으로 다음 4개만 묻는다 (자동 감지된 사실은 open-ended 재질문 금지 — AC3):
    - `plugins/project-init/commands/project-init.md:171` — `docs/git-workflow/` 디렉토리가 없으면 생성. 다음 3개 파일 작성:
    - `/Users/jeonghokim/.claude/plugins/cache/claude-plugins-official/claude-code-setup/1.0.0/skills/claude-automation-recommender/SKILL.md:11` — **This skill is read-only.** It analyzes the codebase and outputs recommendations. It does NOT create or modify any files.
    - `https://code.claude.com/docs/en/best-practices:0` — /init analyzes the codebase and creates a CLAUDE.md starting point.
  - 근거: 공식 /init은 build system·test framework·code pattern을 분석해 CLAUDE.md 초안을 만드는 범용 bootstrap이다. project-init Phase 0도 manifest와 디렉토리를 스캔하므로 tech-stack 탐색 비용은 겹친다. 그러나 project-init은 branching strategy를 선택하고 vision·non-goals·conventions를 확인해 세 Git 문서와 charter detail을 생성한다. claude-code-setup도 코드베이스를 분석하지만 파일을 쓰지 않고 자동화 후보만 추천한다. 따라서 정체성 갭은 아니며, 기존 /init 결과를 입력으로 재사용하는 최적화 여지는 있다.

### OQ4
- 출처: claude
  - 답: Yes — a reversible-only-via-git, irreversible-for-uncommitted-files destruction path exists, and approval prompts cover every path's ACTION but not, in the S4 case, its CONSEQUENCE. (1) Destruction paths enumerated: all destruction is in the LLM-executed prose of commands/project-init.md; both hooks (post-tool-use.py, docs-lint.py) always exit 0 and never Write. CLAUDE.md-mutating paths: S1 create (safe), S2a migrate-and-replace (content preserved by migrating to AGENTS.md), S4(i) rewrite-to-pointer (content DISCARDED, AGENTS.md unchanged), plus C-S2 in-place docs overwrite and the git-workflow 'replace?' overwrite. (2) Prompt coverage is complete at the path level: S2 has the Step-1 migration prompt (L25-27, test-locked as AC21), S4 has its 4c advisory (L165), the git-workflow overwrite has the Step-1 '교체할까요?' prompt (L20-21), and C-S2 has the '업데이트할까요?' prompt (L85). No CLAUDE.md overwrite is unprompted. (3) BUT: there is ZERO filesystem backup on any path — recovery depends entirely on git; the S4(i) advisory text is neither specified nor test-locked and need not disclose that divergent CLAUDE.md content is permanently discarded; and S4(i) directly contradicts the L167 invariant that non-managed content is preserved in all states. So prompts cover the actions, but the S4(i) consent is obtained under a false preservation promise, and no backup exists to recover from either an S4(i) discard or an S2a migration truncation on an uncommitted file.
    - `plugins/project-init/commands/project-init.md:165` — (i) CLAUDE.md를 `@AGENTS.md` 한 줄로 *재작성* (AGENTS.md unchanged), (ii) abort. 승인 시 (i) 수행 + S3 action.
    - `plugins/project-init/commands/project-init.md:167` — 비-관리 컨텐츠 (다른 헤딩, 단락, 코드 블록)는 모든 state에서 보존. 유일한 예외는 4c S2a (d)의 H1 재제목
  - 근거: The security question resolves to: prompt coverage exists everywhere, but for S4(i) it is coverage-without-informed-consent (false L167 safety promise) plus no backup, yielding a deterministic content-loss path (A6-1); and no path has any recoverability preflight, leaving git as the sole safety net that fails for uncommitted content (A6-2). D5 confirms the pointer target is functional, so this is content-loss, not also a broken-import double-harm.
- 출처: codex
  - 답: 되돌릴 수 없는 파괴 위험이 있다. 백업은 없고, 승인 프롬프트는 모든 기존-content 교체 경로를 덮지 않는다.
    - `plugins/project-init/commands/project-init.md:20` — 기존 Git Workflow 설정이 발견되면 사용자에게 묻는다:
    - `plugins/project-init/commands/project-init.md:25` — 또한 root에 `CLAUDE.md`가 존재하고 `AGENTS.md`가 없다면 사용자에게 묻는다:
    - `plugins/project-init/commands/project-init.md:162` — | **S1 (clean slate)** | 없음 | 없음 | AGENTS.md 신규 작성 (`agents-md-section.md` content); CLAUDE.md 신규 작성 (`claude-md-pointer.md` content — `@AGENTS.md` 한 줄). |
    - `plugins/project-init/commands/project-init.md:164` — | **S3 (AGENTS canonical, CLAUDE pointer)** | 존재 | 존재 + `@AGENTS.md` (R-pointer 통과) | AGENTS.md의 `## Git Workflow` 섹션만 in-place 갱신. CLAUDE.md는 unchanged. |
    - `plugins/project-init/commands/project-init.md:169` — #### 4d: docs/git-workflow/ 파일 쓰기
    - `plugins/project-init/commands/project-init.md:205` — | **C-S1 (clean)** | `AGENTS.md`에 `## Project Charter` 섹션 신규 추가(`agents-md-section.md` 치환본) + `docs/project/charter.md`·`conventions.md` 생성 (+용어 있으면 `glossary.md`). |
    - `plugins/project-init/commands/project-init.md:206` — | **C-S2 (complete)** | 업데이트 승인 시: `## Project Charter` 섹션 in-place 교체 + `docs/project/` 파일 in-place 갱신. 거절 시: 전부 unchanged, 중복 `## Project Charter` 섹션 생성 안 함. |
    - `plugins/project-init/commands/project-init.md:207` — | **C-S3 (a) 섹션 항목 누락** | Phase 1 보충 질문으로 채운 뒤 C-S1과 동일하게 발행(in-place 교체). |
  - 근거: write 전수 목록은 S1의 AGENTS.md·CLAUDE.md 신규 생성, S2a의 CLAUDE→AGENTS migration 후 pointer 교체, S2b의 AGENTS 생성, S3의 AGENTS Git Workflow 교체, S4의 CLAUDE 생성·재작성과 S3 action, docs/git-workflow 3파일 작성, C-S1의 charter append 및 docs/project 생성, C-S2 승인 시 charter·detail 갱신, C-S3(a)의 in-place 교체, C-S3(b)의 누락 detail 생성, 조건부 glossary 생성이다. hooks 두 개는 파일을 쓰지 않고 JSON·stderr만 출력한다. 기존 Git rules 감지 프롬프트, S2 migration, S4 pointer rewrite, C-S2 update에는 승인이 있지만 Step 1은 AGENTS.md의 기존 Git Workflow를 직접 검사하지 않고 C-S3(a)는 C-S2 update 승인 없이 교체한다. 어느 write 경로에도 백업·rollback 지시가 없다.
- 이 질문과 관련된 발견: A6-1

### OQ5
- 출처: claude
  - 답: OUT OF SCOPE for this cycle — a separate concern (별건), not a project-init gap. CI absence is a devbrew-repo-level property, explicitly outside LD5 (gap scope = plugins/project-init/**, docs/git-workflow/**, marketplace project-init entry). The brief itself instructs this be answered as a scope judgment only, not a finding. On the merits: project-init already ships its own test infrastructure — the CHANGELOG/README describe smoke.sh as 'V2 자동화 smoke script (CI-runnable, no human eyeballing, macOS bash 3.2 호환)' and the suite is 95/95 green — so the plugin is testable in isolation. What is missing is a repo-wide CI runner that executes those suites automatically on every PR. That is a cross-cutting devbrew effort orthogonal to project-init's identity and to this read-only audit cycle. Verdict: 별건 (separate concern), defer to a devbrew-level CI initiative; do not attribute to project-init.
    - `plugins/project-init/CHANGELOG.md:88` — `hooks/tests/smoke.sh` — V2 자동화 smoke script (CI-runnable, no human eyeballing, macOS bash 3.2 호환).
    - `plugins/project-init/README.md:22` — smoke.sh                # V2 자동화 smoke script
  - 근거: CI absence is repo-level and explicitly out of LD5 gap scope; the plugin ships CI-runnable tests. Answered as scope judgment: 별건, defer to devbrew-wide CI effort.
- 출처: codex
  - 답: devbrew CI 부재는 관측 가능한 운영 위험이지만 LD5 밖이므로 finding으로 올리지 않는다.
    - `plugins/project-init/CHANGELOG.md:88` — - `hooks/tests/smoke.sh` — V2 자동화 smoke script (CI-runnable, no human eyeballing, macOS bash 3.2 호환).
  - 근거: project-init은 95개 unittest와 CI-runnable smoke를 제공하지만 repository-level workflow가 없어 자동 실행 연결은 없다. CI 설정은 plugins/project-init, docs/git-workflow 또는 marketplace project-init 항목에 속하지 않으므로 이번 갭 범위에서는 NOQ다.

### OQ6
- 출처: claude
  - 답: 감사 대상이다 — 단 *검증 가능한 정확성*으로 한정하고, 취향은 제외한다. 3개 branching strategy 템플릿의 콘텐츠는 project-init이 사용자 프로젝트의 *운영 문서*(docs/git-workflow/*.md, AGENTS.md 섹션)로 발행하는 실물이므로, 거기 담긴 사실 주장(git/GitHub 동작, regex 허용 문자, canonical 소스 충실도)이 틀리면 '생성물을 통해 거짓이 사용자 프로젝트로 배포'되는 재현 가능한 실패다. 이 실패는 공식 quality-criteria.md(Currency 항목5 'File references accurate', Actionability 항목6, Red Flags 'inaccurate references')와 권위 있는 1차 출처로 객관 측정된다 — 실제로 이 축에서 세 건의 정확성 주장(GitHub `Closes:` 콜론형, qg `gh pr create` 자동 트리거, trunk-based Pattern B backport)을 authoritative 출처로 판정했고 그중 하나(git-flow:34 'dot 허용 부정')가 confirmed 부정확으로 드러났다. 반면 순수 스타일·문장 취향은 LD6(입증책임) 위반이라 감사하지 않는다 — 재현 실패도 steelman 조건도 없기 때문이다. 요컨대 '구조·정직성'은 바닥(floor)이고, 그 위에 '검증 가능한 콘텐츠 정확성'을 얹는다. 흥미롭게도 quality-criteria로 재면 템플릿은 대체로 통과한다: 사용자 미응답 필드를 `_명시되지 않음._` placeholder로 남기는 project-init의 선택은 quality-criteria Red Flags가 명시적으로 경계하는 'canned generic advice / uncustomized template copy'를 피하는 *옳은* 설계다.
  - 근거: 축5 프롬프트가 '템플릿 *내용* 품질을 quality-criteria.md로 판정하라 — 네 취향이 아니라'와 'trunk-based Pattern B 내용이 실제로 정확한가'를 명시 지시하므로 콘텐츠 정확성은 스코프 안이다. 동시에 계약 4항(LD6 입증책임)이 취향 기반 구조 비판을 무효화하므로 스타일은 스코프 밖이다. 이 경계는 이 축의 실측으로 뒷받침된다: 콘텐츠 정확성 질문 3건이 전부 1차 출처로 결정 가능했고(2건 정확→갭 아님, 1건 부정확→A5-1), 이는 '콘텐츠 정확성은 객관 감사 가능, 취향은 아님'을 실증한다.
- 출처: codex
  - 답: 세 branching strategy 템플릿의 내용 품질은 명백한 감사 대상이다.
    - `plugins/project-init/commands/project-init.md:47` — ### Step 2: Branching Strategy 선택
    - `plugins/project-init/commands/project-init.md:59` — ### Step 3: 커스터마이징 질문
    - `plugins/project-init/commands/project-init.md:111` — #### Phase 1 — 판단 질문 (AskUserQuestion, ≤4개)
    - `plugins/project-init/commands/project-init.md:124` — 각 필수 항목에 대해 빈/무의미 응답이면 AskUserQuestion 재질문을 **최대 3회**까지 한다.
    - `/Users/jeonghokim/.claude/plugins/cache/claude-plugins-official/claude-md-management/1.0.0/skills/claude-md-improver/references/quality-criteria.md:66` — - Commands work as documented
    - `/Users/jeonghokim/.claude/plugins/cache/claude-plugins-official/claude-md-management/1.0.0/skills/claude-md-improver/references/quality-criteria.md:79` — - Commands can be copy-pasted
    - `/Users/jeonghokim/.claude/plugins/cache/claude-plugins-official/claude-md-management/1.0.0/skills/claude-md-improver/references/quality-criteria.md:103` — - Commands that would fail (wrong paths, missing deps)
  - 근거: command가 템플릿을 사용자 프로젝트의 docs/git-workflow로 직접 복제하므로 공식 quality-criteria의 currency·actionability·실행 가능한 명령·정확한 경로 기준이 적용된다. GitHub Flow는 대체로 간결하고 실행 가능하다. Git Flow는 custom release 답변을 반영하지 않고 dot을 허용하는 regex와 '하이픈만' 문장이 충돌한다. Trunk-based는 legacy branch base가 잘못됐다. UX 흐름은 전략 선택 후 공통 2문항, Git Flow만 1문항 추가, charter 최대 4문항과 항목별 최대 3회 재질문이다. Step 1 거절은 전체 abort지만 charter 실패는 Git 산출물을 남기며, 그 조건부 결과를 Step 5가 정확히 보고하지 않는다.
- 이 질문과 관련된 발견: CX-2

## 후보 단서 판정 (D1–D5)
### D1
- 출처: claude — 판정: reclassified
  - 근거: 직접 검증: (레지스트리) installed_plugins.json:84에 commit-commands@claude-plugins-official가 설치돼 있고, 캐시의 commands/commit-push-pr.md·commit.md가 실재하므로 `/commit-push-pr`·`/commit` 권고는 존재하지 않는 명령을 가리키는 '거짓'이 아니다. (b) 그 권고는 templates/shared/pr-process.md:77을 통해 사용자 프로젝트 docs/git-workflow/pr-process.md:77로 실제 복제된다(확인). (a) README에 prerequisites 섹션은 없다. 그러나 CLAUDE.md의 prerequisites 규칙은 `other-plugin:agent-name`을 *dispatch*하는 플러그인을 대상으로 하며, project-init은 dispatch하지 않고 명령을 *추천*만 한다(pr-process.md:77이 '**commit-commands**:' 굵은 헤더로 출처를 명시 = named coupling, silent 아님). 따라서 'silent coupling HIGH 버그'가 아니라, line 78의 '(설치 시)' 조건 표기가 line 77에 빠진 LOW 비일관으로 재분류 — finding A1-2.
  - 영향: 생성물로 복제되지만 출처가 명시된 soft coupling. commit-commands 미설치 사용자에게 예측 가능한 경미한 실패(회복 가능). LOW.
- 출처: codex — 판정: confirmed
  - 근거: 설치 레지스트리 /Users/jeonghokim/.claude/plugins/installed_plugins.json:84-91에서 commit-commands가 실제 설치된 것은 확인했다. 그러나 README에는 prerequisites 섹션이 없고, plugins/project-init/templates/shared/pr-process.md:77이 조건 표시 없이 /commit-push-pr을 사용자 프로젝트에 복제하며 command 완료 메시지도 같은 명령을 권한다.
  - 영향: 현재 감사 호스트에서는 동작하지만 project-init만 설치한 다른 사용자에게 실행 불가능한 명령이 생성물로 배포된다.
  - 수정: optional prerequisite를 문서화하고 설치 여부에 따라 생성물과 완료 메시지를 조건화한다.

### D2
- 출처: claude — 판정: withdrawn
  - 근거: 인덱스가 아니라 본문을 검증: quality-gates/hooks/hooks.json은 PostToolUse(Bash) matcher만 등록해 이벤트 목록엔 PR 트리거가 안 보이지만, 훅 *본문* quality-gates/hooks/post-tool-use.py:54가 `re.search(r"gh\s+pr\s+create", command)`로 PR 생성을 감지하고, stdout에서 PR URL을 찾으면(78행) 'You MUST now initialize the quality-gates pipeline' systemMessage를 주입한다(88-96행). 즉 README:79('quality-gates: PR 생성 시 quality 파이프라인 자동 트리거')와 생성물 pr-process.md:78('gh pr create에 quality 파이프라인 자동 트리거')는 코드와 정합하는 참이다. 계약 6항의 index-vs-body 함정 — 본문이 트리거를 구현하므로 갭 아님.
  - 영향: 허위 주장 없음. project-init README/생성물의 qg 자동 트리거 서술은 정확.
- 출처: codex — 판정: withdrawn
  - 근거: quality-gates hooks.json의 event 이름만으로는 PR trigger가 보이지 않지만 구현은 plugins/quality-gates/hooks/post-tool-use.py:54에서 gh pr create를 검출하고, :78에서 성공 PR URL을 확인한 뒤 :88-95에서 pipeline 초기화 systemMessage를 발행한다. project-init README:79와 pr-process.md:78의 직접 gh pr create 조건부 주장은 구현과 일치한다.

### D3
- 출처: claude — 판정: confirmed
  - 근거: marketplace.json:17('Git workflow initialization: select branching strategy … generate rules and auto-validate.')과 plugin.json:3(charter·fact-routing interview·AGENTS.md/CLAUDE.md·charter integrity 명시)의 description은 실제로 다르다(staleness sweep 확증). drift는 사실. 다만 marketplace 문구의 모든 내용은 참이고 CHANGELOG.md:10이 밝힌 v1.7.1 의도적 압축의 결과라, '거짓'이 아니라 headline 기능(charter/interview) 누락으로 인한 discoverability 불완전 — LOW finding A1-1로 리포트.
  - 영향: 브라우징 사용자가 리스팅에서 charter 기능을 발견 못 함. 거짓 없음. LOW.
- 출처: codex — 판정: confirmed
  - 근거: .claude-plugin/marketplace.json:17은 Git workflow만 설명하지만 plugin.json:3은 필수 fact-routing charter interview와 AGENTS.md/CLAUDE.md·charter integrity를 포함한다. 단순 문자열 차이를 넘어 설치 전 설명에서 필수 질문과 추가 write 범위가 빠져 있다.
  - 영향: 사용자가 설치 카드에서 예상한 범위보다 더 많은 질문과 프로젝트 파일 변경을 실행 중에 처음 접한다.
  - 수정: marketplace 문구를 plugin.json과 의미적으로 동기화한다.

### D4
- 출처: claude — 판정: withdrawn
  - 근거: 3개 파일 직접 확인: 두 개는 dogfooding 중 leak된 qg 세션 인덱스(.claude/quality-gates/<uuid>/files.md), 세 번째는 templates/.claude/… 하위. (1) 전부 .gitignore:214의 `.claude/`(임의 depth)에 매칭돼 untracked → git 배포 패키지에 포함되지 않음. (2) 복제 메커니즘 부재: 템플릿을 *쓰는* 코드(commands/project-init.md:138-143 Step 4a)는 `${CLAUDE_PLUGIN_ROOT}/templates/<strategy>/…`·`templates/shared/…`·`templates/project/…`의 *특정 명명 파일만* Read하고, `templates/`를 재귀/glob 복사하지 않는다. 따라서 templates/.claude/… 오염 파일도 사용자 프로젝트로 복제될 경로가 없다. 사용자 프로젝트로 새는 거짓 없음.
  - 영향: 로컬 워킹트리 cruft(git-ignored). 배포·생성물 누출 없음. 축1 갭 아님(축② 위생 관찰로는 별개).
- 출처: codex — 판정: withdrawn
  - 근거: 세 ignored files를 모두 직접 읽었고 session 파일 목록과 절대·임시 경로가 실제 존재한다. 그러나 .gitignore:214-220이 plugin 하위 .claude를 제외하고, 설치 cache에도 포함되지 않는다. 더 중요하게 command는 plugins/project-init/commands/project-init.md:138-145와 :181-185의 명시적 template 목록만 읽으므로 templates/.claude를 재귀 복제하지 않는다.

### D5
- 출처: claude — 판정: confirmed
  - 근거: The premise 'Claude Code does not natively read AGENTS.md' is CONFIRMED TRUE by the authoritative official documentation as of 2026-07. Claude Code memory docs (code.claude.com/docs/en/memory.md, section 'AGENTS.md') state verbatim: 'Claude Code reads `CLAUDE.md`, not `AGENTS.md`. If your repository already uses `AGENTS.md` for other coding agents, create a `CLAUDE.md` that imports it...' and give the canonical example `@AGENTS.md` inside CLAUDE.md — which is EXACTLY what project-init emits (templates/shared/claude-md-pointer.md:1 = '@AGENTS.md'; commands/project-init.md:134 'AGENTS.md를 canonical content source로, CLAUDE.md를 @AGENTS.md thin pointer로 발행'). Two independent web sources corroborate: bestagent.dev/claude-md-vs-agents-md-2026 ('No. Claude Code does not natively read AGENTS.md as of June 2026') and gist yurukusa/d36197848911f025add142abefcde685 ('Does Claude Code read AGENTS.md? No — it reads CLAUDE.md. The @AGENTS.md import ... actually work'). The long-watched native-support request anthropics/claude-code#34235 remains unresolved. Because the premise is confirmed, project-init's AGENTS.md-canonical + @AGENTS.md-thin-pointer design is not merely defensible — it is the officially-recommended pattern. This is NOT a gap. The brief's rule ('withdrawn이면 갭으로, confirmed면 갭이 아니다') therefore yields 'not a gap'.
  - 영향: Positive for the identity axis: project-init's core file-layout design (AGENTS.md canonical for 16+ vendors, CLAUDE.md = @AGENTS.md import for Claude Code) matches Anthropic's own 2026-07 recommendation verbatim. The README claims at README.md:51-52 ('canonical content source, OpenAI Codex/Cursor/Aider 등 16+ 벤더 자동 인식' / 'Claude Code가 AGENTS.md content를 자동 import') are accurate against current official docs, not stale.
  - 수정: None required. Design is correct as of 2026-07. If native AGENTS.md support ever lands (issue #34235), the CLAUDE.md pointer becomes redundant-but-harmless; a future changelog note would suffice at that point.
- 출처: codex — 판정: confirmed
  - 근거: 2026-07 현재 공식 문서 https://code.claude.com/docs/en/memory 는 Claude Code가 AGENTS.md를 직접 읽지 않으며, 기존 AGENTS.md를 쓰는 저장소에는 이를 import하는 CLAUDE.md를 만들라고 명시한다. 따라서 AGENTS.md canonical + CLAUDE.md의 @AGENTS.md pointer 설계는 현재 공식 권고와 일치한다. /init이 AGENTS.md를 탐색해 CLAUDE.md에 관련 내용을 반영할 수 있는 것과 runtime native loading은 별개다.
  - 영향: D5 자체는 갭이 아니다.

## 열린 질문 (NOQ — 갭은 아니나 조용히 버리지 않는다)
- **NOQ-c1** (축4): README:77이 'commit-commands: /commit과 /commit-push-pr이 CLAUDE.md 룰을 읽어 메시지 포맷 적용'이라 서술하나, 캐시의 commit.md·commit-push-pr.md는 CLAUDE.md를 *명시적으로 읽지 않는다*(context에 git status/diff/branch/log만 주입하고 'appropriate message' 작성만 지시). 또한 v1.4.0 이후 규칙은 CLAUDE.md가 아니라 AGENTS.md(canonical)에 살며 CLAUDE.md는 `@AGENTS.md` 포인터다. 따라서 '메커니즘' 서술이 이중으로 부정확하다. — *왜 갭이 아닌가*: 효과는 사실상 성립한다 — CLAUDE.md(+@AGENTS.md import)는 모든 Claude Code 세션 컨텍스트에 상시 로드되므로 /commit 실행 시 모델이 컨벤션을 컨텍스트에서 참조해 적용한다. README는 명령의 파일-read가 아니라 사용자-관측 효과를 서술한 것으로 방어 가능하다. 게다가 이건 사용자 프로젝트로 복제되지 않는 plugin-layer README 문구이며, 정확히는 형제 플러그인의 *메커니즘 서술 정확성* 문제라 축④(의존성/커플링 서술) 소관에 가깝다. 확정 갭으로 승격하기엔 반대근거가 강해 관찰로 남긴다.
- **NOQ-c2** (축2): README:98과 command Step 3.5(project-init.md:122-128)가 charter 완결을 '진행을 막는' 'clarity 구조 게이트'로 서술하나, 실제 enforcement는 command 마크다운 안의 모델-준수 지시문일 뿐이며 결정론적 강제가 없다(docs-lint의 R-charter는 advisory·non-blocking·사후 플래그). CLAUDE.md Law 1은 '구조적 게이트 = silent하게 skip할 수 없어야 함'으로 정의하는데, 이 게이트는 모델이 조용히 건너뛸 수 있다. — *왜 갭이 아닌가*: command 파일에는 그 게이트 지시문이 실제로 존재하고, README/command가 사후 advisory 성격(docs-lint 사후 플래그, 3회 후 loud abort, git-workflow는 독립 진행)을 정직하게 disclose한다. '이 게이트가 CLAUDE.md 정의상 충분히 structural한가'라는 판정은 축②(게이트 강도)의 소관이며, 축①이 포치하지 않도록 관찰로만 surface한다. 산문이 코드 동작에 대해 명백히 '거짓'을 말한다고 단정하기엔 disclosure가 충분해 갭으로 올리지 않는다.
- **NOQ-c3** (축2): 파일-상태 분류의 결정론 로직이 코드와 산문에 이중 존재한다. docs-lint.py는 AC16 pointer-정규화(`_normalize_pointer_content`:294-306)·proper-pointer 판정(`_is_proper_pointer`:309-336)·charter-섹션 추출(`CHARTER_SECTION_RE`/`_charter_field`)을 *구현·테스트*하는데, command는 같은 분류를 산문으로 재진술한다(Step1 charter 감지 :35-43; 4c S2 셀 'AC16 정규화 절차 (frontmatter strip → HTML comment strip → str.strip()) 준용해 분류' :163). 모델은 코드가 이미 결정론적으로 계산하는 상태를 런타임에 재도출해야 한다. 향후 command가 Bash로 호출하는 작은 classifier로 통합하면 드리프트를 없앨 수 있으나, 코드와 산문이 어긋나 잘못된 분기를 낸 재현 사례는 아직 없다. — *왜 갭이 아닌가*: 구조/드리프트 비판(형제와 다르다·중복이다)은 LD6에서 명시적으로 논거가 아니다. 재현 가능한 실패 모드도, 충족된 steelman(b/d)도 없다. P8은 오히려 선제적 결정론화를 경계한다. 따라서 갭이 아니라 관찰.
- **NOQ-c4** (축2): 생성 로직(S1–S4 AGENTS/CLAUDE 매트릭스 + C-S1..C-S3 charter 매트릭스, 사용자 파일 mutation)은 *행위* 테스트가 0이다. test_command_contract.py는 산문 규칙의 *존재*만 잠그며(4개 문구), docstring이 'a test cannot exercise its behaviour'라고 명시한다. 실제로 산문-로직 버그(v1.7.2 H1 재제목)가 릴리스로 탈출했고, 유닛/스모크가 아니라 out-of-band WS5 sandbox 재실행으로만 잡혔다. 이는 steelman (b)('v1.7.2류 버그가 반복적 규칙-부재 패턴으로 재발')의 tripwire다. — *왜 갭이 아닌가*: 탈출한 버그는 이미 잡혀 수정됐고(현행 갭 아님), steelman (b)는 단일 사례가 아니라 *반복* 패턴을 요구한다 — 현재 명백한 산문-로직 버그 인스턴스는 v1.7.2 1건뿐이라 조건 미충족. 재발 감시 항목으로 기록. 두 번째 유사 규칙-부재 버그가 나오면 (b)가 충족되어 classifier 스크립트화가 정당화된다.
- **NOQ-c5** (축2): 4c S4(i) 경로는 divergent content를 가진 CLAUDE.md를 그 고유 내용을 AGENTS.md로 이전하지 *않고* `@AGENTS.md` 한 줄로 재작성한다(commands:165). S2a와 달리 S4는 AGENTS.md가 이미 canonical이라는 전제로 CLAUDE.md의 divergent 내용을 stale로 간주 — 사용자가 AGENTS.md에 없는 고유 섹션을 CLAUDE.md에 뒀다면 S4(i) 승인 시 그 내용이 소실될 수 있다. 이는 산문-해석이 되돌리기 어려운 mutation을 수행하는 표면으로, steelman (c)(PreToolUse급 게이트 필요 여부)의 입력이다. — *왜 갭이 아닌가*: 이 mutation은 (1) 명시적 사용자 advisory+승인 게이트 뒤에 있고(silent 아님), (2) AGENTS.md/CLAUDE.md는 git-tracked라 회복 가능하다 — 따라서 '되돌릴 수 없는 silent overwrite' 등급인지의 파괴-severity 판정은 축6 OQ4 소관이지 축2가 단독 결정할 사안이 아니다. 축6이 (c)를 확증하면 OQ1 steelman이 pending→c로 해소된다. 축2 단독으로는 갭이 아니라 관찰.
- **NOQ-c6** (축3): docs-lint.py declares several accepted false-positive / false-negative surfaces: tilde (~~~) fences are not linted (line 164), R6 does not exclude links inside fenced code blocks / inline backtick spans (lines 242-244), and R-charter matches a `**Label:**` line even inside a fenced code block within the charter section (lines 419-421). These are real gaps in rule precision. — *왜 갭이 아닌가*: Each is explicitly documented in-code as a deliberate scope-out accepted 'for parity with R5/R6', the hook is advisory/non-blocking, and behavior matches its documented scope — so there is no user deception and no reproducible wrong outcome beyond the disclosed limits. This is a documented design trade-off, not a hidden capability failure. Recorded so it is not silently dropped.
- **NOQ-c7** (축3): The github-flow branch-strategy template states '**NEVER** `main`에 직접 commit' as a hard rule for Claude, but neither hook enforces base-branch protection: post-tool-use.py validates only the commit MESSAGE format, never which branch HEAD is on, so committing directly to main is never flagged. — *왜 갭이 아닌가*: This is an explicitly deferred non-goal, not a missing feature: CHANGELOG.md:40 records base-branch/merge runtime enforcement (F4/F5) as deliberately deferred under 'harness lightness — trust the model', and no plugin doc claims the hook enforces branch protection (README scopes the hook to branch-NAME and commit-FORMAT advisories). No false claim, no reproducible deception. Recorded to preserve the observation.
- **NOQ-c8** (축3): smoke.sh asserts only that the 'bad' fixtures produce a 'systemMessage' substring (line 92 `grep -q "$expected"`) and that 'good' fixtures produce exactly '{}' — it never checks WHICH rule fired. A fixture that triggered the wrong rule (e.g. an R6 link error where an R-pointer drift was intended) would still pass the smoke check. — *왜 갭이 아닌가*: The per-rule teeth live in test_docs_lint.py, which asserts specific rule strings ('drift risk', 'unresolved internal link', 'section is incomplete', line-number formats). smoke.sh is an intentionally coarse end-to-end presence check layered on top of the fine-grained unit suite, so the coarseness is not a capability gap. Recorded because it looked like a toothless proxy on first read but is backed by teeth elsewhere.
- **NOQ-c9** (축4): The built-in /init gained an interactive multi-phase flow (CLAUDE_CODE_NEW_INIT=1, per official code.claude.com/docs/en/memory.md) that 'asks which artifacts to set up: CLAUDE.md files, skills, and hooks ... explores your codebase with a subagent, fills in gaps via follow-up questions, and presents a reviewable proposal before writing any files.' This overlaps project-init's Phase 0 (manifest scan) + Phase 1 (AskUserQuestion elicitation) noticeably more than the README/command acknowledge — neither project-init doc mentions /init at all. — *왜 갭이 아닌가*: No reproducible failure mode: project-init still produces AGENTS.md-canonical git-workflow templates, docs/git-workflow/, branch/commit enforcement hooks, and a durable Project Charter that /init does not, and it writes the opposite (officially-recommended) canonical direction. Per LD6, a structural overlap without a concrete wrong outcome is an observation, not a gap. Surfaced so the orchestrator can decide whether a one-line README acknowledgment of /init is worth a docs patch.
- **NOQ-c10** (축4): project-init's docs-lint.py enforces root-context-file conventions (size ≤200, TOC >300, fenced-code language, internal links resolve, CLAUDE/AGENTS drift, charter fields). Official Claude Code tooling now covers adjacent ground: the /doctor checkup (v2.1.206+) 'proposes trims for a checked-in CLAUDE.md' (official memory docs), and Anthropic ships an official claude-md-management plugin for CLAUDE.md quality. There is conceptual overlap with docs-lint's ruleset. — *왜 갭이 아닌가*: Modality differs and is genuinely additive: docs-lint is a deterministic PostToolUse hook that fires on every Write/Edit (enforcement at the action layer), whereas /doctor is on-demand and claude-md-management is a model-invoked skill — neither guarantees firing on every edit. No reproducible failure and no steelman condition met, so this is an external-comparison observation, not a gap (LD6). Note: the docs-lint mechanism itself is primarily an enforcement-axis concern, cited here only for the external-tooling comparison relevant to identity.
- **NOQ-c11** (축5): templates/shared/pr-process.md:77 'commit-commands: 간소화된 PR 생성을 위해 `/commit-push-pr` 사용'은 바로 아래 line 78('quality-gates (설치 시): …')과 달리 '설치 시' 같은 조건 한정어가 없다. 이 파일은 docs/git-workflow/pr-process.md로 사용자 프로젝트에 복제되므로(project-init.md:175), commit-commands 미설치 사용자는 자기 운영 문서에서 존재하지 않는 `/commit-push-pr`을 무조건 안내받는다. 같은 doc 내부의 조건-한정 문구 비일관(line 77 무한정 vs line 78 '설치 시')은 축5(콘텐츠 내부 일관성) 관점의 LOW 관찰이다. — *왜 갭이 아닌가*: 실질 피해(미설치 시 깨진 명령 안내)의 최종 판정 — 이것이 '미선언 의존성/Silent coupling' 위반인지 — 은 후보 단서 D1이 소유한 의존성 선언 축의 primary 판정이라, 그 축의 독립성 보존을 위해 축5에서 갭으로 승격하지 않고 관찰로만 남긴다(계약 5: 다른 축 발견 추측 금지). 내부 문구 일관성 나aught 자체는 A5-1보다도 약해 별도 finding 가치가 낮다.
- **NOQ-c12** (축5): 결정론 staleness sweep가 trunk-based/branch-strategy.md:109('release 브랜치를 새 기능 개발에 쓰지 말 것 — 그 순간 long-lived feature branch가 되어 TBD 위반')를 '[draft residue]'로 관측 나열했다. 나는 직접 원문을 읽고 이것이 residue가 아니라 line 108('release 브랜치는 receive-only')을 보완해 *왜*(long-lived branch=TBD 위반)를 설명하는 완결된 유효 규칙이라고 판정한다. — *왜 갭이 아닌가*: 문장이 문법적으로 완결이고 TBD canonical에 부합하며, quality-criteria.md 항목3(Non-Obvious Patterns, 'Why we do it this way for unusual patterns')이 오히려 가치를 인정하는 종류의 콘텐츠다. line 108과 약한 강조-중복은 있으나 108(무엇)+109(왜)로 상보적이라 Red Flag 'redundancy'에 해당하지 않는다. 갭 아님.
- **NOQ-c13** (축5): commands/project-init.md는 상태 파일(.claude/<plugin>.local.md)이나 명시적 중단·재개 메커니즘이 없는 단일 인터랙티브 명령이다. Step 4 중간(4c AGENTS/CLAUDE 갱신, 4d docs/git-workflow/ 쓰기, 4e charter 발행)에서 중단되면 부분 산출물이 남고 롤백이 없다. — *왜 갭이 아닌가*: 재현 가능한 실패가 아니다: 명령이 설계상 멱등이다. Step 1 감지 + 4c의 S1–S4 matrix(project-init.md:160-165) + charter C-S1/C-S2/C-S3 matrix(project-init.md:41-43, 203-208)가 '이미 부분 완료' 상태를 감지해 in-place로 라우팅하므로 중단 후 재실행이 곧 복구 경로다(예: charter 섹션만 있고 docs 누락 → C-S3(b)가 질문 없이 누락 파일 생성). 별도 resume 상태 파일이 없어도 matrix가 recovery를 제공하므로 갭 아님. plugin-shape의 state-file 규범은 런타임 상태를 갖는 hook/skill 대상이지 일회성 생성 명령의 강제 요건이 아니다.
- **NOQ-c14** (축5): 사용자가 conventions/charter 질문에 희소하게 답하면 생성된 docs/project/conventions.md·charter.md의 여러 헤딩이 `_명시되지 않음._` / `_명시되지 않음 — 추후 보강._` 마커로 채워질 수 있다(project-init.md:195-199, 208). quality-criteria.md Red Flags는 'TODO items never completed'를 경계 항목으로 든다. — *왜 갭이 아닌가*: 이는 결함이 아니라 quality-criteria가 요구하는 방향의 *옳은* 선택이다. 같은 Red Flags가 'Copy-paste from templates without customization'과 'Generic advice not specific to the project'를 더 강하게 경계하는데, project-init은 의견 콘텐츠 주입을 금지(AC14)하고 사용자 elicited 값만 캡처한다. 빈 필드를 canned 문구로 채우는 대안이 오히려 Red Flag이므로, 정직한 placeholder 마커는 trade-off의 정답이다. 재현 실패 없음 → 갭 아님.
- **NOQ-c15** (축6): The git-workflow overwrite path (Step 4d writes docs/git-workflow/branch-strategy.md, commit-conventions.md, pr-process.md) is a wholesale file overwrite, not a section-merge. If a user hand-edited those generated files and then approves the Step-1 '기존 git workflow 룰이 감지됐습니다. 새 템플릿으로 교체할까요?' prompt (L20-21), their edits are replaced with no backup. — *왜 갭이 아닌가*: This overwrite is gated by an explicit 'replace?' prompt whose refusal aborts (L23), and the files are plugin-template-shaped generated artifacts (docs/git-workflow/*) rather than the user's primary hand-authored context file, so the expected-value content loss is low and consent is clear. It does not rise to a destruction gap on its own, but it shares A6-2's root cause (no backup on overwrite) and would be fixed by the same preflight.
- **NOQ-c16** (축6): The three git-ignored quality-gates session files under the plugin (and under templates/.claude/) embed the author's local absolute paths (e.g. /Users/jeonghokim/Downloads/devbrew/... and /private/tmp/claude-501/...). — *왜 갭이 아닌가*: They are git-ignored (excluded from the published tarball) and are never read/copied by the command (Step 4a reads only named templates/<strategy|shared|project> files), so they cannot leak into a user project or the marketplace and create no destruction path. The residual working-tree-hygiene concern belongs to axis 4, not the axis-6 destruction lens.
- **NOQ-x1** (축1): git-ignored quality-gates session 파일 3개는 실제로 존재하며 사용자 절대 경로와 임시 scratch 경로를 담고 있다. — *왜 갭이 아닌가*: 모두 .gitignore의 plugin-state 규칙에 의해 제외되고 설치 cache에도 없으며, 생성 command는 template 파일을 명시적으로 열거해 templates/.claude를 읽거나 재귀 복제하지 않는다. 따라서 현재 사용자 프로젝트로 전파되는 실패 모드가 없다.
- **NOQ-x2** (축1): quality-gates의 hooks event 이름에는 PR 전용 trigger가 없지만 hook 본문은 직접 gh pr create 성공을 검출한다. — *왜 갭이 아닌가*: 구현이 성공 PR URL을 확인하고 quality pipeline 초기화를 지시하므로 project-init의 조건부 통합 설명은 참이다. 인덱스만 본 D2 가설이 철회됐다.
- **NOQ-x3** (축1): staleness sweep의 dangling doc-claim 27건은 역사적 삭제 기록, 사용자 프로젝트의 생성 대상 경로, pointer 본문을 파일 참조로 오인한 항목으로 분류됐다. — *왜 갭이 아닌가*: 예를 들어 llm-guidelines.md는 현존한다고 주장한 것이 아니라 제거 사실을 기록한 것이고, README의 @AGENTS.md는 import 본문이다. 전수 대조에서 현재 구현이 dereference하는 존재하지 않는 파일은 발견되지 않았다.
- **NOQ-x4** (축1): sweep의 draft residue인 TBD는 출하 미완료 마커처럼 보인다. — *왜 갭이 아닌가*: 해당 위치의 TBD는 바로 앞 문맥에서 Trunk-Based Development를 가리키는 약어이며 TODO·미결정 값이 아니다.
- **NOQ-x5** (축1): 95/95 GREEN 중 command contract 테스트는 실제 command 수행이 아니라 산문 존재를 검증한다. — *왜 갭이 아닌가*: 테스트와 CHANGELOG가 이 한계를 명시적으로 밝히므로 테스트 자체가 실행 능력을 거짓 주장하지는 않는다. 지원 hook 입력과 kill switch는 실제 Python 테스트가 검증하지만 모델이 산문을 정확히 따를지는 별도 sandbox 실행이 필요하다.
- **NOQ-x6** (축4): claude-code-setup도 codebase를 스캔하므로 project-init과 표면적으로 겹친다. — *왜 갭이 아닌가*: 공식 skill은 read-only로 hook·skill·MCP·subagent 자동화를 추천할 뿐 파일을 생성하지 않는다. project-init은 선택한 Git 정책과 사용자에게서 받은 charter를 프로젝트 파일로 발행하므로 산출물과 책임이 다르다.
- **NOQ-x7** (축4): devbrew에는 project-init 테스트를 자동 실행하는 repository CI가 없다. — *왜 갭이 아닌가*: 운영상 의미는 있지만 CI 설정 위치는 LD5 밖이다. 이번 감사 계약에 따라 finding으로 승격하지 않았다.
- **NOQ-x8** (축6): command write contract에는 생성 대상의 realpath가 project root 안인지 확인하는 명시적 경계가 없다. — *왜 갭이 아닌가*: symlink된 AGENTS.md 또는 docs 디렉토리를 Write/Edit가 실제로 어떻게 처리하는지 read-only 환경에서 재현할 수 없고, tool runtime의 symlink 정책도 범위 파일에 정의돼 있지 않다. 구체적 외부-path overwrite를 입증하지 못했으므로 구조 변경 finding이 아니라 unverified NOQ로 남긴다.

