# quality-gates v1.32.3 follow-up Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (사용자 선택: INLINE mode per spec §9). Checkbox(`- [ ]`) 문법으로 진행 추적.

**Goal:** PR #71 deferred 6건(MED-1..4, I-C, I-D)을 단일 PR로 v1.32.2 → v1.32.3에 ship.

**Architecture:** 신규 helper 3개(`read-frontmatter.py` MED-3 / `check-allowed-tools-order.sh` I-D / `check-changelog-korean-primary.py` I-C) + 신규 test 3개 + 신규 fixture 1개 + 기존 6개 파일 in-place 수정. TDD per task (test → implement → verify → commit).

**Tech Stack:** Bash (set -euo pipefail), Python 3 (stdlib only, regex 기반), git CLI, 기존 quality-gates plugin 구조.

**Spec reference:** [`docs/superpowers/specs/2026-05-28-qg-v1322-followup-design.md`](../specs/2026-05-28-qg-v1322-followup-design.md) (v1.2.0, approved 2026-05-28).

**Worktree absolute path** (모든 Edit/Write에 적용): `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup`

---

## 목차

- [Task 0: Preflight](#task-0-preflight)
- [Task 1: MED-3 helper (read-frontmatter.py + unit test)](#task-1-med-3-helper)
- [Task 2: MED-3 call-site transition (3 sites)](#task-2-med-3-call-site-transition)
- [Task 3: MED-1 + MED-4 (cancel-qg-core.sh hardening)](#task-3-med-1--med-4)
- [Task 4: MED-2 (pre-pipeline-check SID boundary tests)](#task-4-med-2)
- [Task 5: I-D (SKILL.md allowed-tools 재정렬 + linter + test)](#task-5-i-d)
- [Task 6: I-C (CHANGELOG [1.32.0] Korean-primary + verifier)](#task-6-i-c)
- [Task 7: 전체 regression run](#task-7-regression)
- [Task 8: Final bump (plugin.json + CHANGELOG [1.32.3])](#task-8-final-bump)
- [Self-Review checklist](#self-review)

---

## Task 0: Preflight

**Purpose**: v1.32.2 baseline 확정, 기존 testsuite green 확인. Plan 실행 중 regression 발생 시 origin 파악 가능.

**Files**: read-only 검사.

- [ ] **Step 0.1: 현재 brunch + 작업 위치 확인**

Run:
```bash
cd /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup && \
  git rev-parse --abbrev-ref HEAD && \
  git log --oneline -3
```
Expected: `feature/qg-v1322-followup` 출력, 최상단 3 commit이 spec 작성 commit들.

- [ ] **Step 0.2: 기존 testsuite green 확인 (baseline)**

Run (각 명령 PASS 출력 검증):
```bash
cd /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup && \
  bash plugins/quality-gates/tests/test_session_start_advisor_v2.sh && \
  bash plugins/quality-gates/tests/test_cancel_qg.sh && \
  bash plugins/quality-gates/tests/test_pre_pipeline_check.sh && \
  bash plugins/quality-gates/tests/harness/test_skill_orchestration_behavior.sh
```
Expected: 4개 모두 마지막 줄 `All tests pass.` 또는 동등 success indicator.

만약 하나라도 실패 → 작업 중단, baseline 오염 디버깅 후 재개. **이 단계가 실패하면 v1.32.2 자체 regression이며 plan 진행 불가.**

- [ ] **Step 0.3: v1.32.2 시작 확인**

Run:
```bash
cat /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/.claude-plugin/plugin.json
```
Expected: `"version": "1.32.2"`.

---

## Task 1: MED-3 helper

**Spec ref**: §4.3 (helper 도입), AC3 + AC4.

**Files:**
- Create: `plugins/quality-gates/scripts/read-frontmatter.py`
- Create: `plugins/quality-gates/tests/test_read_frontmatter.sh`

- [ ] **Step 1.1: 실패하는 unit test 먼저 작성**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_read_frontmatter.sh`:

```bash
#!/usr/bin/env bash
# v1.32.3 MED-3: read-frontmatter.py helper 5-case unit test.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
HELPER="$ROOT/quality-gates/scripts/read-frontmatter.py"

TMP="$(mktemp -d)"
trap 'rm -rf "$TMP"' EXIT

fail=0
run_case() {
  local name="$1" fixture="$2" key="$3" expected="$4"
  local fixture_file="$TMP/$name.md"
  printf '%s\n' "$fixture" > "$fixture_file"
  local actual
  actual="$(python3 "$HELPER" "$fixture_file" "$key" 2>/dev/null)"
  if [[ "$actual" == "$expected" ]]; then
    echo "  PASS $name"
  else
    echo "  FAIL $name: expected [$expected] got [$actual]"
    fail=$((fail + 1))
  fi
}

echo "--- T-RF-quoted ---"
run_case "T-RF-quoted" '---
key: "value"
---' "key" "value"

echo "--- T-RF-unquoted ---"
run_case "T-RF-unquoted" '---
key: value
---' "key" "value"

echo "--- T-RF-missing ---"
run_case "T-RF-missing" '---
other: foo
---' "key" ""

echo "--- T-RF-embedded-quote ---"
# Fixture에 literal \" 시퀀스 — escape-aware regex가 val"ue로 해석.
run_case "T-RF-embedded-quote" '---
key: "val\"ue"
---' "key" 'val"ue'

echo "--- T-RF-embedded-backslash ---"
# Fixture에 literal \\ 시퀀스 — single backslash로 해석.
run_case "T-RF-embedded-backslash" '---
key: "a\\b"
---' "key" 'a\b'

if [[ "$fail" -eq 0 ]]; then
  echo "All tests pass."
  exit 0
else
  echo "$fail test(s) failed."
  exit 1
fi
```

- [ ] **Step 1.2: 실행 권한 부여 + 실패 확인**

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_read_frontmatter.sh && \
  bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_read_frontmatter.sh
```
Expected: FAIL with "No such file or directory" 또는 `python3: can't open file ... /scripts/read-frontmatter.py` (helper 아직 없음).

- [ ] **Step 1.3: helper 구현**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/read-frontmatter.py`:

```python
#!/usr/bin/env python3
"""Read a single frontmatter value from a YAML-frontmatter markdown file.

Usage: read-frontmatter.py <file> <key>
Stdout: value (without surrounding quotes; \\" / \\\\ escape 해제), 또는
        key 부재 시 빈 줄.
Exit: 0 on success (key 부재도 success), 1 on file/parse error.

v1.32.3 MED-3: 3 call site (pre-pipeline-check.sh × 2, cancel-qg-core.sh × 1)
의 `awk -F'"'` 패턴을 대체. escape-aware regex로 embedded quote 처리.
"""
from __future__ import annotations

import re
import sys
from pathlib import Path


def main() -> int:
    if len(sys.argv) != 3:
        print("usage: read-frontmatter.py <file> <key>", file=sys.stderr)
        return 1

    path, key = Path(sys.argv[1]), sys.argv[2]
    try:
        text = path.read_text(encoding="utf-8")
    except OSError as e:
        print(f"read-frontmatter: {e}", file=sys.stderr)
        return 1

    # Frontmatter는 `---` 마커로 감싸짐. 없으면 전체 텍스트 대상.
    m = re.search(r"^---\s*\n(.*?)\n---\s*\n", text, re.DOTALL)
    fm = m.group(1) if m else text

    # Match key: "...escaped..." (escape-aware) OR key: bare-value
    # Escape-aware quoted: `[^"\\]` (일반 char) 또는 `\\.` (escape sequence) 반복.
    m2 = re.search(
        rf'^{re.escape(key)}:\s*(?:"((?:[^"\\]|\\.)*)"|(.*))$',
        fm,
        re.MULTILINE,
    )
    if m2:
        if m2.group(1) is not None:  # quoted form
            # Unescape minimal YAML double-quoted escape sequences:
            # \" → ", \\ → \. (현재 schema 범위: 한 줄 값만)
            val = (
                m2.group(1)
                .replace("\\\\", "\x00")
                .replace('\\"', '"')
                .replace("\x00", "\\")
            )
            print(val)
        else:  # bare form
            print(m2.group(2).strip())
    else:
        print("")  # key 부재 — 명시적 빈 줄
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 1.4: helper 실행 권한 부여 + test PASS 확인**

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/read-frontmatter.py && \
  bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_read_frontmatter.sh
```
Expected: 5 케이스 모두 `PASS`, 마지막 줄 `All tests pass.`, exit 0.

**만약 T-RF-embedded-quote가 FAIL이면**: regex가 escape sequence를 잡지 못함 — 구현 line의 `(?:[^"\\]|\\.)*` 패턴 검토.

- [ ] **Step 1.5: Commit**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/scripts/read-frontmatter.py \
  plugins/quality-gates/tests/test_read_frontmatter.sh && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
feat(quality-gates): add read-frontmatter.py helper (MED-3)

frontmatter `key: "value"` 파싱 helper 신규. escape-aware regex로
embedded `\"` 와 `\\` sequence 처리. AC4 5 케이스 PASS:
- T-RF-quoted / T-RF-unquoted / T-RF-missing
- T-RF-embedded-quote (val"ue 정상 반환)
- T-RF-embedded-backslash (a\b 정상 반환)

다음 task에서 3 call site (pre-pipeline-check.sh × 2,
cancel-qg-core.sh × 1) 의 `awk -F'"'` 패턴을 이 helper로 전환.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: MED-3 call-site transition

**Spec ref**: §4.3 (3 call sites 전환), AC3.

**Files:**
- Modify: `plugins/quality-gates/scripts/pre-pipeline-check.sh:47,56`
- Modify: `plugins/quality-gates/scripts/cancel-qg-core.sh:57`

- [ ] **Step 2.1: pre-pipeline-check.sh에 SCRIPT_DIR 정의 추가**

기존 파일에는 SCRIPT_DIR 미정의. line 17(`set -euo pipefail`) 다음에 추가.

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/pre-pipeline-check.sh`:

old_string:
```
set -euo pipefail

SESSION_ID="${CLAUDE_CODE_SESSION_ID:-}"
```

new_string:
```
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

SESSION_ID="${CLAUDE_CODE_SESSION_ID:-}"
```

- [ ] **Step 2.2: pre-pipeline-check.sh line 47 (branch 파싱) 전환**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/pre-pipeline-check.sh`:

old_string:
```
  last_branch="$(awk -F'"' '/^branch:/ {print $2; exit}' "$BRANCH_FILE" 2>/dev/null || echo "")"
```

new_string:
```
  last_branch="$(python3 "$SCRIPT_DIR/read-frontmatter.py" "$BRANCH_FILE" branch 2>/dev/null || echo "")"
```

- [ ] **Step 2.3: pre-pipeline-check.sh line 56 (session_id 파싱) 전환 + tr -d 제거**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/pre-pipeline-check.sh`:

old_string:
```
    pipeline_session=$(awk -F'"' '/^session_id:/ { print $2; exit }' "$STATE_FILE" 2>/dev/null | tr -d '[:space:]')
```

new_string:
```
    pipeline_session=$(python3 "$SCRIPT_DIR/read-frontmatter.py" "$STATE_FILE" session_id 2>/dev/null)
```

(helper가 `.strip()` 적용하므로 `tr -d` 불필요 — spec advisory 3.)

- [ ] **Step 2.4: cancel-qg-core.sh에 SCRIPT_DIR 정의 추가**

기존 파일 line 69에 `script_dir=...` 정의 있으나, lowercase `script_dir`이고 line 57(worktree_path 파싱)보다 *뒤*에 정의됨. line 57에서 사용 가능하도록 file top으로 끌어올림 (`set -euo pipefail` 다음).

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh`:

old_string:
```
set -euo pipefail

session_id=""
```

new_string:
```
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

session_id=""
```

- [ ] **Step 2.5: cancel-qg-core.sh line 57 (worktree_path 파싱) 전환**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh`:

old_string:
```
  worktree_path=$(awk -F'"' '/^worktree_path:/ { print $2; exit }' "$target_dir/pipeline.md" 2>/dev/null)
```

new_string:
```
  worktree_path=$(python3 "$SCRIPT_DIR/read-frontmatter.py" "$target_dir/pipeline.md" worktree_path 2>/dev/null)
```

- [ ] **Step 2.6: cancel-qg-core.sh 내 기존 lowercase `script_dir` 사용처 통일**

기존 코드의 line 69~80에서 lowercase `script_dir`를 사용. 이미 line 4에서 우리가 `SCRIPT_DIR`(uppercase)을 정의했으므로 일관성 위해 lowercase 정의는 삭제하고 사용처는 SCRIPT_DIR 참조.

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh`:

old_string:
```
if [[ -n "$worktree_path" && -d "$worktree_path" ]]; then
  script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  if [[ -x "$script_dir/qg-worktree.sh" ]]; then
    if "$script_dir/qg-worktree.sh" remove "$worktree_path" 2>&1 \
```

new_string:
```
if [[ -n "$worktree_path" && -d "$worktree_path" ]]; then
  if [[ -x "$SCRIPT_DIR/qg-worktree.sh" ]]; then
    if "$SCRIPT_DIR/qg-worktree.sh" remove "$worktree_path" 2>&1 \
```

Edit 같은 파일 again:

old_string:
```
    echo "cancel-qg-core: qg-worktree.sh missing or not executable at $script_dir — worktree at $worktree_path not removed; clean it manually" >&2
```

new_string:
```
    echo "cancel-qg-core: qg-worktree.sh missing or not executable at $SCRIPT_DIR — worktree at $worktree_path not removed; clean it manually" >&2
```

(이 메시지는 Task 3에서 다시 강화되므로 임시 변경 OK.)

- [ ] **Step 2.7: AC3 검증**

Run:
```bash
grep -rn "awk -F'\"'" /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/
```
Expected: 출력 없음 (0 hits).

Run:
```bash
grep -c "SCRIPT_DIR=" /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/pre-pipeline-check.sh /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh
```
Expected: 각각 `:1` (한 줄씩 정의됨).

- [ ] **Step 2.8: 기존 regression test 재실행**

Run:
```bash
cd /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup && \
  bash plugins/quality-gates/tests/test_pre_pipeline_check.sh && \
  bash plugins/quality-gates/tests/test_cancel_qg.sh && \
  bash plugins/quality-gates/tests/test_read_frontmatter.sh
```
Expected: 3개 모두 PASS. helper 전환이 기존 동작을 깨지 않았음 확인.

- [ ] **Step 2.9: Commit**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/scripts/pre-pipeline-check.sh \
  plugins/quality-gates/scripts/cancel-qg-core.sh && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
refactor(quality-gates): MED-3 transition 3 call sites to read-frontmatter helper

`awk -F'"' '/^key:/ {print $2}'` 패턴 3 site → read-frontmatter.py 호출.
SCRIPT_DIR 변수를 pre-pipeline-check.sh와 cancel-qg-core.sh 양쪽에
file top (set -euo pipefail 직후)에 정의해 helper 경로 확보.

- pre-pipeline-check.sh:47 (branch 파싱)
- pre-pipeline-check.sh:56 (session_id 파싱) — tr -d '[:space:]' 제거
  (helper가 .strip() 내부 적용, advisory 3 흡수)
- cancel-qg-core.sh:57 (worktree_path 파싱)
- cancel-qg-core.sh의 lowercase script_dir 정의 삭제, SCRIPT_DIR 통일

AC3 충족: grep -c "awk -F'\"'" plugins/quality-gates/scripts/ == 0.
기존 test_pre_pipeline_check.sh + test_cancel_qg.sh regression 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: MED-1 + MED-4

**Spec ref**: §4.1 (MED-1 메시지 강화), §4.4 (MED-4 sed 제거 + set -e safety), AC1 + AC5.

**Files:**
- Modify: `plugins/quality-gates/scripts/cancel-qg-core.sh`
- Create: `plugins/quality-gates/tests/fixtures/qg-worktree-fail-stub.sh`
- Create: `plugins/quality-gates/tests/test_cancel_qg_med4.sh`

- [ ] **Step 3.1: MED-4 — sed pipe 제거 + if/else set -e safety**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh`:

old_string:
```
  if [[ -x "$SCRIPT_DIR/qg-worktree.sh" ]]; then
    if "$SCRIPT_DIR/qg-worktree.sh" remove "$worktree_path" 2>&1 \
        | sed 's/^/cancel-qg-core: worktree: /' >&2; then
      :
    else
      echo "cancel-qg-core: worktree removal failed (continuing with state-folder cleanup)" >&2
    fi
  else
```

new_string:
```
  if [[ -x "$SCRIPT_DIR/qg-worktree.sh" ]]; then
    # MED-4: sed pipe 제거. set -euo pipefail 활성 상태이므로
    # `var=$(failing_cmd)` 시 즉시 exit 위험. if/else로 exit code 명시 캡처.
    # qg-worktree.sh 출력 계약 보존: stdout+stderr 병합 스트림을 prefix-emit.
    if worktree_output="$("$SCRIPT_DIR/qg-worktree.sh" remove "$worktree_path" 2>&1)"; then
      worktree_rc=0
    else
      worktree_rc=$?
    fi
    if [[ -n "$worktree_output" ]]; then
      while IFS= read -r line; do
        printf 'cancel-qg-core: worktree: %s\n' "$line" >&2
      done <<< "$worktree_output"
    fi
    if [[ "$worktree_rc" -ne 0 ]]; then
      echo "cancel-qg-core: qg-worktree.sh remove exit code $worktree_rc (continuing with state-folder cleanup)" >&2
    fi
  else
```

- [ ] **Step 3.2: MED-1 — qg-worktree.sh 부재 시 메시지 self-actionable화**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh`:

old_string:
```
    echo "cancel-qg-core: qg-worktree.sh missing or not executable at $SCRIPT_DIR — worktree at $worktree_path not removed; clean it manually" >&2
  fi
fi
```

new_string:
```
    # MED-1: missing vs not-executable 구별 + 사용자 직접 실행 명령 명시.
    if [[ ! -e "$SCRIPT_DIR/qg-worktree.sh" ]]; then
      echo "cancel-qg-core: qg-worktree.sh MISSING at $SCRIPT_DIR/" >&2
    else
      mode="$(stat -f %Lp "$SCRIPT_DIR/qg-worktree.sh" 2>/dev/null || stat -c %a "$SCRIPT_DIR/qg-worktree.sh" 2>/dev/null)"
      echo "cancel-qg-core: qg-worktree.sh EXISTS but not executable at $SCRIPT_DIR/qg-worktree.sh (mode: $mode)" >&2
    fi
    echo "cancel-qg-core: orphan worktree at $worktree_path — clean manually with:" >&2
    echo "  git worktree remove --force \"$worktree_path\"" >&2
  fi
fi
```

- [ ] **Step 3.3: fixture 생성 + 실행 권한**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/fixtures/qg-worktree-fail-stub.sh`:

```bash
#!/usr/bin/env bash
# v1.32.3 MED-4 test fixture: simulates qg-worktree.sh remove failure.
echo "stub: simulated worktree removal failure" >&2
exit 1
```

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/fixtures/qg-worktree-fail-stub.sh
```

- [ ] **Step 3.4: test_cancel_qg_med4.sh 생성**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_cancel_qg_med4.sh`:

```bash
#!/usr/bin/env bash
# v1.32.3 MED-4 test: cancel-qg-core.sh가 qg-worktree.sh 실패 시
# 정확한 exit code 메시지를 stderr에 출력하는지 검증.
# 비정상 종료 시에도 stub이 원본을 덮어쓰지 않도록 trap EXIT으로 복원.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
SCRIPTS="$ROOT/quality-gates/scripts"
FIXTURE="$ROOT/quality-gates/tests/fixtures/qg-worktree-fail-stub.sh"
ORIGINAL="$SCRIPTS/qg-worktree.sh"
BACKUP="$SCRIPTS/qg-worktree.sh.med4-backup"

# Backup + install stub
mv "$ORIGINAL" "$BACKUP"
trap 'mv -f "$BACKUP" "$ORIGINAL" 2>/dev/null || true' EXIT
cp "$FIXTURE" "$ORIGINAL"
chmod +x "$ORIGINAL"

# Sandbox: tempdir에 SID 폴더 + pipeline.md 만들고 호출.
SANDBOX="$(mktemp -d)"
SID="med4-test-sid-deadbeef"
mkdir -p "$SANDBOX/.claude/quality-gates/$SID"
mkdir -p "$SANDBOX/fake-worktree-path"
cat > "$SANDBOX/.claude/quality-gates/$SID/pipeline.md" <<EOF
---
session_id: "$SID"
started_at: "2026-05-28T00:00:00Z"
worktree_path: "$SANDBOX/fake-worktree-path"
---
EOF

cd "$SANDBOX"

# cancel-qg-core 호출, stderr capture.
stderr_capture="$SANDBOX/stderr.log"
bash "$SCRIPTS/cancel-qg-core.sh" --session-id "$SID" 2>"$stderr_capture" || true

# 검증: stub의 메시지 + exit code 라인이 stderr에 prefix-emit됐는지.
fail=0
if grep -q 'cancel-qg-core: worktree: stub: simulated worktree removal failure' "$stderr_capture"; then
  echo "PASS: stub failure message prefix-emitted"
else
  echo "FAIL: stub message not prefixed properly"; echo "--- stderr ---"; cat "$stderr_capture"
  fail=$((fail + 1))
fi
if grep -q 'cancel-qg-core: qg-worktree.sh remove exit code 1' "$stderr_capture"; then
  echo "PASS: exit code 1 explicitly logged"
else
  echo "FAIL: exit code line missing"
  fail=$((fail + 1))
fi
# sed 의존 0건 (소스 정적 검증, advisory level)
sed_count="$(grep -c "sed" "$SCRIPTS/cancel-qg-core.sh" || true)"
if [[ "$sed_count" -eq 0 ]]; then
  echo "PASS: zero sed reference in cancel-qg-core.sh"
else
  echo "FAIL: $sed_count sed reference(s) remain in cancel-qg-core.sh"
  fail=$((fail + 1))
fi

rm -rf "$SANDBOX"

if [[ "$fail" -eq 0 ]]; then
  echo "All tests pass."
  exit 0
else
  echo "$fail test(s) failed."
  exit 1
fi
```

- [ ] **Step 3.5: test 실행 권한 + 실행**

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_cancel_qg_med4.sh && \
  bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_cancel_qg_med4.sh
```
Expected: 3 PASS 라인, 마지막 `All tests pass.`, exit 0.

**복원 확인**:
```bash
ls -la /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/qg-worktree.sh*
```
Expected: `qg-worktree.sh` (executable, 원본 복원). `qg-worktree.sh.med4-backup`은 trap이 mv했으므로 부재.

- [ ] **Step 3.6: MED-1 검증용 manual run (AC1)**

Run (MISSING 케이스):
```bash
cd /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup && \
  SANDBOX="$(mktemp -d)" && SID="ac1-test-sid-12345678" && \
  mkdir -p "$SANDBOX/.claude/quality-gates/$SID" && \
  mkdir -p "$SANDBOX/fake-wt" && \
  printf '%s\n' '---' "session_id: \"$SID\"" 'started_at: "2026-05-28T00:00:00Z"' "worktree_path: \"$SANDBOX/fake-wt\"" '---' > "$SANDBOX/.claude/quality-gates/$SID/pipeline.md" && \
  ORIG="plugins/quality-gates/scripts/qg-worktree.sh" && \
  mv "$ORIG" "$ORIG.bak" && \
  cd "$SANDBOX" && \
  bash "/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh" --session-id "$SID" 2>&1 | tee out.log
  cd - && \
  mv "$ORIG.bak" "$ORIG" && \
  rm -rf "$SANDBOX"
```
Expected stderr (in `out.log`): `cancel-qg-core: qg-worktree.sh MISSING at <...>/scripts/` + `git worktree remove --force "<sandbox>/fake-wt"` 라인.

Run (not-executable 케이스):
```bash
cd /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup && \
  ORIG="plugins/quality-gates/scripts/qg-worktree.sh" && \
  chmod -x "$ORIG" && \
  SANDBOX="$(mktemp -d)" && SID="ac1b-test-sid-12345678" && \
  mkdir -p "$SANDBOX/.claude/quality-gates/$SID" && \
  mkdir -p "$SANDBOX/fake-wt" && \
  printf '%s\n' '---' "session_id: \"$SID\"" 'started_at: "2026-05-28T00:00:00Z"' "worktree_path: \"$SANDBOX/fake-wt\"" '---' > "$SANDBOX/.claude/quality-gates/$SID/pipeline.md" && \
  cd "$SANDBOX" && \
  bash "/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/cancel-qg-core.sh" --session-id "$SID" 2>&1 | tee out2.log
  cd - && \
  chmod +x "$ORIG" && \
  rm -rf "$SANDBOX"
```
Expected stderr (in `out2.log`): `cancel-qg-core: qg-worktree.sh EXISTS but not executable at <...>/qg-worktree.sh (mode: ...)` + `git worktree remove --force` 라인.

- [ ] **Step 3.7: Commit**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/scripts/cancel-qg-core.sh \
  plugins/quality-gates/tests/fixtures/qg-worktree-fail-stub.sh \
  plugins/quality-gates/tests/test_cancel_qg_med4.sh && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
fix(quality-gates): MED-1 + MED-4 cancel-qg-core hardening

MED-1: qg-worktree.sh 부재/비실행 메시지 self-actionable화.
- MISSING vs EXISTS-but-not-executable 구별 (mode 출력)
- 사용자 실행 명령 명시: `git worktree remove --force "<path>"`
- exit code 0 유지 (state folder 정리는 계속)

MED-4: sed pipe 제거 + set -e safety.
- `cmd | sed` 대신 stdout/stderr 병합 capture + 수동 prefix
- if/else 형태로 exit code 명시 캡처 ($worktree_rc)
- qg-worktree.sh 출력 계약 보존 (병합 스트림 prefix-emit)

신규 fixture: tests/fixtures/qg-worktree-fail-stub.sh (영구, exit 1).
신규 test: tests/test_cancel_qg_med4.sh
- mv backup + cp stub + trap EXIT 자동 복원 (symlink 폐기)
- 3 assertion: stub 메시지 prefix / exit code 1 라인 / sed 0건

AC1 (MED-1), AC5 (MED-4) 충족. 기존 test_cancel_qg.sh regression 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: MED-2

**Spec ref**: §4.2 (SID guard boundary tests), AC2.

**Files:**
- Modify: `plugins/quality-gates/tests/test_pre_pipeline_check.sh` (append cases)

- [ ] **Step 4.1: 기존 test 파일 확인**

Run:
```bash
ls -la /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_pre_pipeline_check.sh
```

만약 파일 부재 → 신규 생성. 만약 존재 → 끝에 SID 케이스 append.

```bash
head -30 /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_pre_pipeline_check.sh
```
이 출력을 읽어 파일 끝 직전의 적절한 append 지점 파악.

- [ ] **Step 4.2: 4 boundary 케이스 append**

기존 파일 끝(마지막 PASS 또는 exit 라인 직전)에 다음 블록 추가:

```bash
# v1.32.3 MED-2: SID guard boundary cases.
echo "--- T-SID-empty ---"
SANDBOX_E="$(mktemp -d)"
( cd "$SANDBOX_E" && git init -q && git commit --allow-empty -q -m "init" )
out="$(CLAUDE_CODE_SESSION_ID="" bash "$ROOT/quality-gates/scripts/pre-pipeline-check.sh" 2>&1)" || true
rc=$?
if echo "$out" | grep -q "^result: no_session_id"; then
  echo "  PASS T-SID-empty (exit $rc)"
else
  echo "  FAIL T-SID-empty: $out"; exit 1
fi
rm -rf "$SANDBOX_E"

echo "--- T-SID-short ---"
SANDBOX_S="$(mktemp -d)"
( cd "$SANDBOX_S" && git init -q && git commit --allow-empty -q -m "init" )
out="$(cd "$SANDBOX_S" && CLAUDE_CODE_SESSION_ID="abcd123" bash "$ROOT/quality-gates/scripts/pre-pipeline-check.sh" 2>&1)" || true
if echo "$out" | grep -q "^result: invalid_session_id" && echo "$out" | grep -q "fails pattern guard"; then
  echo "  PASS T-SID-short"
else
  echo "  FAIL T-SID-short: $out"; exit 1
fi
rm -rf "$SANDBOX_S"

echo "--- T-SID-invalid-char ---"
SANDBOX_I="$(mktemp -d)"
( cd "$SANDBOX_I" && git init -q && git commit --allow-empty -q -m "init" )
out="$(cd "$SANDBOX_I" && CLAUDE_CODE_SESSION_ID="abc/def123" bash "$ROOT/quality-gates/scripts/pre-pipeline-check.sh" 2>&1)" || true
if echo "$out" | grep -q "^result: invalid_session_id" && echo "$out" | grep -q "fails pattern guard"; then
  echo "  PASS T-SID-invalid-char"
else
  echo "  FAIL T-SID-invalid-char: $out"; exit 1
fi
rm -rf "$SANDBOX_I"

echo "--- T-SID-valid ---"
# Sandbox isolation: temp git repo + fresh state (no .claude/quality-gates/$SID/).
# SID guard만 통과하는 deterministic 환경.
SANDBOX_V="$(mktemp -d)"
( cd "$SANDBOX_V" && git init -q && git commit --allow-empty -q -m "init" )
out="$(cd "$SANDBOX_V" && CLAUDE_CODE_SESSION_ID="abc-def_123ABC" bash "$ROOT/quality-gates/scripts/pre-pipeline-check.sh" 2>&1)" || true
rc=$?
if echo "$out" | grep -q "^result: fresh_start"; then
  echo "  PASS T-SID-valid (rc=$rc)"
else
  echo "  FAIL T-SID-valid: $out"; exit 1
fi
rm -rf "$SANDBOX_V"
```

(이 블록은 기존 파일의 `ROOT="..."` 변수와 호환됨을 가정. 만약 변수명이 다르면 grep으로 확인 후 조정.)

- [ ] **Step 4.3: test 실행**

Run:
```bash
bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_pre_pipeline_check.sh
```
Expected: 기존 케이스 + 4 new (T-SID-empty / T-SID-short / T-SID-invalid-char / T-SID-valid) 모두 PASS.

- [ ] **Step 4.4: Commit**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/tests/test_pre_pipeline_check.sh && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
test(quality-gates): MED-2 SID guard boundary cases for pre-pipeline-check

4 boundary 케이스 추가:
- T-SID-empty: 빈 SID → result: no_session_id, exit 1
- T-SID-short: 7 char (< 8 min) → invalid_session_id + pattern guard
- T-SID-invalid-char: `abc/def123` → invalid_session_id + pattern guard
- T-SID-valid: 15 char 유효 SID + sandbox git init + fresh state → fresh_start

T-SID-valid는 deterministic 위해 sandbox tempdir + git init + 빈 state.
다른 guard (branch mismatch, staleness) 발화 회피.

AC2 충족. 기존 케이스 regression 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: I-D

**Spec ref**: §4.6 (SKILL.md frontmatter pipeline-order + linter + test), AC7 + AC8.

**Files:**
- Modify: `plugins/quality-gates/skills/quality-pipeline/SKILL.md` (frontmatter)
- Create: `plugins/quality-gates/scripts/check-allowed-tools-order.sh`
- Create: `plugins/quality-gates/tests/test_check_allowed_tools_order.sh`

- [ ] **Step 5.1: SKILL.md allowed-tools 재정렬**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/skills/quality-pipeline/SKILL.md`:

old_string:
```
allowed-tools:
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/run_codex_reviewer.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/synthesize_findings.py:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/scout.py:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-qg.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/pre-pipeline-check.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/check-trivia.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/detect-runtime.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/compute-test-scope-candidates.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/detect_codex.sh:*)
  - Agent
  - AskUserQuestion
  - Edit
  - Write
  - Read
  - Glob
  - Grep
```

new_string:
```
allowed-tools:
  # Group 1 — Preflight scripts (실행 순서: setup → pre-check → trivia)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-qg.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/pre-pipeline-check.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/check-trivia.sh:*)
  # Group 2 — Gate 2 PR review scripts
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/scout.py:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/run_codex_reviewer.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/synthesize_findings.py:*)
  # Group 3 — Gate 3 runtime verification scripts
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/detect-runtime.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/detect_codex.sh:*)
  - Bash(${CLAUDE_PLUGIN_ROOT}/scripts/compute-test-scope-candidates.sh:*)
  # Group 4 — Meta (orchestration primitives)
  - Agent
  - AskUserQuestion
  # Group 5 — File operations
  - Read
  - Glob
  - Grep
  - Edit
  - Write
```

- [ ] **Step 5.2: linter 생성**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-allowed-tools-order.sh`:

```bash
#!/usr/bin/env bash
# v1.32.3 I-D: SKILL.md allowed-tools pipeline-order linter.
# Canonical source: 이 스크립트 내부 EXPECTED_ORDER 배열 (single source of truth).
# SKILL.md의 `# Group N — ...` 주석은 *문서화*이지 canonical 아님.
# Drift 감지 시 SKILL.md를 이 linter에 맞춰 수정.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
SKILL="$ROOT/quality-gates/skills/quality-pipeline/SKILL.md"

EXPECTED_ORDER=(
  # Group 1 — Preflight scripts
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup-qg.sh:*)'
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/pre-pipeline-check.sh:*)'
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/check-trivia.sh:*)'
  # Group 2 — Gate 2 PR review scripts
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/scout.py:*)'
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/run_codex_reviewer.sh:*)'
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/synthesize_findings.py:*)'
  # Group 3 — Gate 3 runtime verification scripts
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/detect-runtime.sh:*)'
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/detect_codex.sh:*)'
  'Bash(${CLAUDE_PLUGIN_ROOT}/scripts/compute-test-scope-candidates.sh:*)'
  # Group 4 — Meta
  'Agent'
  'AskUserQuestion'
  # Group 5 — File operations
  'Read'
  'Glob'
  'Grep'
  'Edit'
  'Write'
)

# Extract allowed-tools list from SKILL.md frontmatter (between `---` markers).
# 각 줄에서 `  - <item>` 패턴 추출. 주석 라인(`  # ...`)은 skip.
mapfile -t ACTUAL < <(
  awk '
    /^---$/ { d++; next }
    d == 1 && /^[[:space:]]*-[[:space:]]/ {
      sub(/^[[:space:]]*-[[:space:]]*/, "")
      print
    }
  ' "$SKILL"
)

if [[ "${#ACTUAL[@]}" -ne "${#EXPECTED_ORDER[@]}" ]]; then
  echo "check-allowed-tools-order: tool count mismatch — expected ${#EXPECTED_ORDER[@]}, found ${#ACTUAL[@]}" >&2
  exit 1
fi

fail=0
for i in "${!EXPECTED_ORDER[@]}"; do
  if [[ "${ACTUAL[$i]}" != "${EXPECTED_ORDER[$i]}" ]]; then
    echo "check-allowed-tools-order: position $i: expected '${EXPECTED_ORDER[$i]}', found '${ACTUAL[$i]}'" >&2
    fail=$((fail + 1))
  fi
done

# Unknown tool 감지 (canonical에 없는 항목이 actual에 있으면 길이 check에서 잡힘).
# Verify all actual items are in expected set (defense-in-depth).
for actual_item in "${ACTUAL[@]}"; do
  found=0
  for exp in "${EXPECTED_ORDER[@]}"; do
    if [[ "$actual_item" == "$exp" ]]; then
      found=1; break
    fi
  done
  if [[ "$found" -eq 0 ]]; then
    echo "check-allowed-tools-order: unexpected tool: $actual_item" >&2
    fail=$((fail + 1))
  fi
done

if [[ "$fail" -eq 0 ]]; then
  echo "check-allowed-tools-order: OK (${#EXPECTED_ORDER[@]} tools in canonical order)"
  exit 0
else
  echo "check-allowed-tools-order: $fail issue(s) — update SKILL.md to match linter EXPECTED_ORDER" >&2
  exit 1
fi
```

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-allowed-tools-order.sh
```

- [ ] **Step 5.3: canonical SKILL.md에 대해 linter PASS 확인**

Run:
```bash
bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-allowed-tools-order.sh
```
Expected: `check-allowed-tools-order: OK (16 tools in canonical order)`, exit 0.

만약 FAIL이면 SKILL.md 재정렬 (Step 5.1)이 linter EXPECTED_ORDER와 일치하지 않음 — 둘을 동기화.

- [ ] **Step 5.4: linter 단위 테스트 생성**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_check_allowed_tools_order.sh`:

```bash
#!/usr/bin/env bash
# v1.32.3 I-D: check-allowed-tools-order.sh 4-scenario unit test.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
LINTER="$ROOT/quality-gates/scripts/check-allowed-tools-order.sh"
SKILL="$ROOT/quality-gates/skills/quality-pipeline/SKILL.md"

TMP="$(mktemp -d)"
trap 'rm -rf "$TMP"' EXIT

fail=0

# Scenario A: T-LA-canonical (현재 SKILL.md → PASS)
echo "--- T-LA-canonical ---"
if bash "$LINTER" >/dev/null 2>&1; then
  echo "  PASS T-LA-canonical"
else
  echo "  FAIL T-LA-canonical (canonical should PASS)"; fail=$((fail + 1))
fi

# Scenarios B/C/D: tmpdir에 SKILL.md copy → mutate → linter override를 위해
# linter 내 ROOT 계산을 우회할 수 없으므로 SKILL.md 원본을 직접 backup/swap.
SKILL_BACKUP="$TMP/SKILL.md.backup"
cp "$SKILL" "$SKILL_BACKUP"
restore() { cp -f "$SKILL_BACKUP" "$SKILL"; }
trap 'restore; rm -rf "$TMP"' EXIT

# Scenario B: T-LA-within-group-swap (Group 5 안에서 Read와 Glob 위치 교환)
echo "--- T-LA-within-group-swap ---"
sed -E '/^  - Read$/,/^  - Glob$/{
  s/^  - Read$/  - __TMP__/
  s/^  - Glob$/  - Read/
  s/^  - __TMP__$/  - Glob/
}' "$SKILL_BACKUP" > "$SKILL"
if bash "$LINTER" >/dev/null 2>&1; then
  echo "  FAIL T-LA-within-group-swap (should FAIL)"; fail=$((fail + 1))
else
  echo "  PASS T-LA-within-group-swap (linter rejected swap)"
fi
restore

# Scenario C: T-LA-cross-group-move (AskUserQuestion을 마지막으로 이동)
echo "--- T-LA-cross-group-move ---"
awk '
  /^  - AskUserQuestion$/ { saved=$0; next }
  /^---$/ && seen_end == 0 && d == 1 { d=2; print saved; print; seen_end=1; next }
  /^---$/ { d++; print; next }
  { print }
' "$SKILL_BACKUP" > "$SKILL"
if bash "$LINTER" >/dev/null 2>&1; then
  echo "  FAIL T-LA-cross-group-move (should FAIL)"; fail=$((fail + 1))
else
  echo "  PASS T-LA-cross-group-move (linter rejected reorder)"
fi
restore

# Scenario D: T-LA-unknown-tool (마지막 group에 Notify 추가)
echo "--- T-LA-unknown-tool ---"
sed -E '/^  - Write$/a\
  - Notify
' "$SKILL_BACKUP" > "$SKILL"
if bash "$LINTER" >/dev/null 2>&1; then
  echo "  FAIL T-LA-unknown-tool (should FAIL)"; fail=$((fail + 1))
else
  echo "  PASS T-LA-unknown-tool (linter rejected unknown tool)"
fi
restore

if [[ "$fail" -eq 0 ]]; then
  echo "All tests pass."
  exit 0
else
  echo "$fail test(s) failed."
  exit 1
fi
```

- [ ] **Step 5.5: 실행 권한 + test 실행**

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_check_allowed_tools_order.sh && \
  bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/tests/test_check_allowed_tools_order.sh
```
Expected: 4 PASS 라인, 마지막 `All tests pass.`, exit 0. SKILL.md 원본 복원 확인:
```bash
bash /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-allowed-tools-order.sh
```
다시 exit 0.

- [ ] **Step 5.6: Commit**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/skills/quality-pipeline/SKILL.md \
  plugins/quality-gates/scripts/check-allowed-tools-order.sh \
  plugins/quality-gates/tests/test_check_allowed_tools_order.sh && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
feat(quality-gates): I-D allowed-tools pipeline-order convention + linter

SKILL.md frontmatter `allowed-tools` 재정렬 (5 groups):
1. Preflight scripts (setup, pre-check, trivia)
2. Gate 2 PR review (scout, codex_reviewer, synthesize_findings)
3. Gate 3 runtime (detect-runtime, detect_codex, compute-scope)
4. Meta (Agent, AskUserQuestion)
5. File ops (Read, Glob, Grep, Edit, Write)

신규: scripts/check-allowed-tools-order.sh (~80 lines)
- Canonical source: 내부 EXPECTED_ORDER 배열 (single source of truth)
- SKILL.md 주석은 문서화이지 canonical 아님
- 새 도구 추가 시 linter EXPECTED_ORDER와 SKILL.md 동시 수정 필요

신규: tests/test_check_allowed_tools_order.sh (4 scenarios)
- T-LA-canonical (현재 SKILL.md → PASS)
- T-LA-within-group-swap (그룹 내 swap → FAIL)
- T-LA-cross-group-move (그룹 간 이동 → FAIL)
- T-LA-unknown-tool (canonical 외 도구 추가 → FAIL)

AC7 + AC8 충족.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: I-C

**Spec ref**: §4.5 (CHANGELOG [1.32.0] Korean-primary), AC6.

**Files:**
- Modify: `plugins/quality-gates/CHANGELOG.md` ([1.32.0] body)
- Create: `plugins/quality-gates/scripts/check-changelog-korean-primary.py`

- [ ] **Step 6.1: 기존 [1.32.0] 본문 추출 (변환 대상 확인)**

Run:
```bash
awk '/^## \[1\.32\.0\]/{flag=1; next} /^## \[/ && flag{exit} flag' /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/CHANGELOG.md
```
출력에 `### Breaking` / `### Added` / `### Changed` / `### Removed` / `### Migration` 5 subsection의 영어 prose 확인.

- [ ] **Step 6.2: [1.32.0] body를 Korean-primary로 in-place edit**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/CHANGELOG.md`. 한 섹션씩 수정.

**Breaking 섹션** — old_string (literal verbatim from current file):
```
### Breaking
- **Stop hook removed.** `hooks/stop-hook.py` (1205 LOC, 13-transition
  state machine, wall-clock guard, no-signal counter) deleted along with
  the `Stop` event registration in `hooks.json`. Pipeline progression now
  lives entirely in the `quality-pipeline` SKILL as in-turn serial
  dispatch.
- **`<qg-signal>` emission contract removed.** SKILL no longer emits the
  signal tag. The `# QG-STOP-HOOK-CONTINUATION` sentinel is no longer
  recognized by any code path.
- **State file shape changed.** v1.32.0 state file is minimal: `session_id`,
  `started_at`, `worktree_path` (optional), `gate2_iteration`. Removed
  fields: `status`, `current_gate`, `consecutive_no_signal`,
  `max_gate2_iterations`, `gate3_resolution_iter`, `last_gate3_needed_hash`,
  `max_gate3_resolutions`, `skip_runtime`, `single_gate`, `plan_file`,
  `pr_url`, `available_plugins`, `wall_clock_deadline_at`, `project_dir`.
- **Env vars removed.** `DEVBREW_QG_DEADLINE_MIN` and
  `DEVBREW_QG_NO_SIGNAL_MAX` no longer exist (wall-clock guard and
  no-signal counter went away with stop-hook). Other env vars unchanged.
```

new_string:
```
### Breaking
- **Stop hook 제거.** `hooks/stop-hook.py` (1205 LOC, 13-transition state
  machine, wall-clock guard, no-signal counter)가 `hooks.json`의 `Stop`
  event 등록과 함께 삭제됨. Pipeline progression은 이제 `quality-pipeline`
  SKILL 안에서 in-turn serial dispatch로 전적으로 처리됨.
- **`<qg-signal>` emission contract 제거.** SKILL이 더 이상 signal tag를
  emit하지 않음. `# QG-STOP-HOOK-CONTINUATION` sentinel은 어떤 코드 경로에서도
  인식되지 않음.
- **State file shape 변경.** v1.32.0 state file은 minimal: `session_id`,
  `started_at`, `worktree_path` (optional), `gate2_iteration`. 제거된 필드:
  `status`, `current_gate`, `consecutive_no_signal`,
  `max_gate2_iterations`, `gate3_resolution_iter`, `last_gate3_needed_hash`,
  `max_gate3_resolutions`, `skip_runtime`, `single_gate`, `plan_file`,
  `pr_url`, `available_plugins`, `wall_clock_deadline_at`, `project_dir`.
- **Env vars 제거.** `DEVBREW_QG_DEADLINE_MIN`과
  `DEVBREW_QG_NO_SIGNAL_MAX`가 더 이상 존재하지 않음 (wall-clock guard와
  no-signal counter는 stop-hook과 함께 사라짐). 다른 env vars는 미변경.
```

**Added 섹션** — old_string:
```
### Added
- **AskUserQuestion progression primitive.** SKILL calls AskUserQuestion
  at Gate 1 FAIL, Gate 2 iter boundary (every iteration), Gate 2 max-iter
  (replacing silent halt), and Gate 3 NEEDS_RESOLUTION. Same-turn tool
  result drives the next dispatch.
- **Static SKILL orchestration test:** `tests/test_skill_orchestration.sh`
  (V2a gate-order + V2b context-anchor + V7 PASS-proximity heuristic).
- **Fixture test for /cancel-qg, /qg --reset, /qg --gc:**
  `tests/test_cancel_qg.sh`.
- **Session-start advisor v2 test:** `tests/test_session_start_advisor_v2.sh`
  (V8 legacy advisory + V8-pre code-structure guard).
```

new_string:
```
### Added
- **AskUserQuestion progression primitive.** SKILL이 Gate 1 FAIL, Gate 2
  iter boundary (매 iteration), Gate 2 max-iter (silent halt 대체), Gate 3
  NEEDS_RESOLUTION에서 AskUserQuestion 호출. Same-turn tool 결과가 다음
  dispatch를 구동.
- **Static SKILL orchestration test:** `tests/test_skill_orchestration.sh`
  (V2a gate-order + V2b context-anchor + V7 PASS-proximity heuristic).
- **`/cancel-qg`, `/qg --reset`, `/qg --gc` fixture test:**
  `tests/test_cancel_qg.sh`.
- **Session-start advisor v2 test:** `tests/test_session_start_advisor_v2.sh`
  (V8 legacy advisory + V8-pre code-structure guard).
```

**Changed 섹션** — old_string:
```
### Changed
- **SKILL.md rewritten** from single-gate-per-turn to single-turn-serial
  dispatch with AskUserQuestion gating.
- **setup-qg.sh** emits minimal state schema; wall-clock and gate3-max
  computation removed.
- **session-start-advisor.py** drops in-flight pipeline detection;
  detects legacy v1.x state files and emits one-shot `/cancel-qg` stderr
  advisory. Frontmatter scan sub-feature unchanged.
- **commands/qg.md** Pipeline Rules section rewritten; removed "Stop hook
  handles progression" claim.
- **README.md** Hook table no longer lists stop-hook.py; state diagram
  replaced with ASCII single-turn sequence; Principles section adds
  P22 generalization note.
```

new_string:
```
### Changed
- **SKILL.md** single-gate-per-turn에서 AskUserQuestion gating의
  single-turn-serial dispatch로 재작성.
- **setup-qg.sh**가 minimal state schema를 emit; wall-clock과 gate3-max
  계산 제거.
- **session-start-advisor.py**는 in-flight pipeline detection을 drop;
  legacy v1.x state file을 감지해 one-shot `/cancel-qg` stderr advisory
  emit. Frontmatter scan sub-feature는 미변경.
- **commands/qg.md** Pipeline Rules 섹션 재작성; "Stop hook handles
  progression" 주장 제거.
- **README.md** Hook 테이블이 더 이상 stop-hook.py를 나열하지 않음;
  state diagram은 ASCII single-turn sequence로 교체; Principles 섹션에
  P22 일반화 노트 추가.
```

**Removed 섹션** — old_string:
```
### Removed
- `hooks/stop-hook.py`
- `hooks/hooks.json` Stop event block
- All `<qg-signal>` references in SKILL/scripts/hooks
- Obsolete tests coupled to stop-hook semantics (`test_forward_only_prose.sh`
  and any stop-hook-coupled tests detected during Task 7)
```

new_string:
```
### Removed
- `hooks/stop-hook.py`
- `hooks/hooks.json`의 `Stop` event block
- SKILL / scripts / hooks 의 모든 `<qg-signal>` 참조
- stop-hook semantics에 coupled된 obsolete test
  (`test_forward_only_prose.sh` + Task 7에서 감지된 stop-hook-coupled test)
```

**Migration 섹션** — old_string:
```
### Migration
v1.x in-flight pipelines cannot resume under v1.32.0. After upgrade, run
`/cancel-qg` (per-session) or `/qg --reset` (legacy flat files) to clear
old state. SessionStart advisor will guide you on next session start.
```

new_string:
```
### Migration
v1.x in-flight pipeline은 v1.32.0에서 resume 불가. 업그레이드 후
`/cancel-qg` (per-session) 또는 `/qg --reset` (legacy flat files)을
실행해 옛 state를 clear. SessionStart advisor가 다음 session 시작 시
guide를 emit함.
```

- [ ] **Step 6.3: verifier 스크립트 생성**

Create `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-changelog-korean-primary.py`:

```python
#!/usr/bin/env python3
"""v1.32.3 I-C: CHANGELOG [1.32.0] body Korean-primary verifier.

Usage: check-changelog-korean-primary.py <CHANGELOG.md>
Exit: 0 if [1.32.0] body satisfies Korean-primary 단락 단위 rule, 1 otherwise.

Rule: 각 non-empty 단락(blank-line 분리)이 다음 중 하나:
- (a) Hangul 문자 (U+AC00..U+D7A3) 1개 이상 포함
- (b) Verbatim 인용 (전체가 백틱/따옴표로 둘러싸인 영어)
- (c) 100% identifier-like (영문자/숫자/`_/-./` + 백틱만)

영구 보존 스크립트 — 향후 CHANGELOG 항목 추가 시에도 동일 컨벤션 재검증 가능.
"""
from __future__ import annotations

import re
import sys
from pathlib import Path

HANGUL_RE = re.compile(r"[가-힣]")
IDENTIFIER_ONLY_RE = re.compile(r"^[\sA-Za-z0-9_\-./`'\"\\(\)\[\],:;*+=<>!?]+$")


def extract_section(text: str, header: str) -> str:
    """Extract body of `## [header]` until next `## [` or EOF."""
    lines = text.splitlines()
    start = -1
    for i, line in enumerate(lines):
        if line.startswith(f"## [{header}]"):
            start = i + 1
            break
    if start == -1:
        return ""
    body: list[str] = []
    for line in lines[start:]:
        if line.startswith("## ["):
            break
        body.append(line)
    return "\n".join(body)


def check_paragraph(p: str) -> bool:
    """Return True if paragraph satisfies Korean-primary rule."""
    p = p.strip()
    if not p:
        return True
    # 헤더 라인 (### Breaking 등)은 면제
    if p.startswith("### "):
        return True
    # (a) Hangul 포함
    if HANGUL_RE.search(p):
        return True
    # (c) 100% identifier-like (간단한 문장 부호 + ascii)
    if IDENTIFIER_ONLY_RE.match(p):
        return True
    # (b) Verbatim 인용: 전체가 백틱 또는 따옴표로 둘러싸였는가 → 간단 휴리스틱
    if p.startswith("`") and p.endswith("`"):
        return True
    if p.startswith('"') and p.endswith('"'):
        return True
    return False


def main() -> int:
    if len(sys.argv) != 2:
        print("usage: check-changelog-korean-primary.py <CHANGELOG.md>", file=sys.stderr)
        return 1

    path = Path(sys.argv[1])
    if not path.exists():
        print(f"check-changelog-korean-primary: {path} not found", file=sys.stderr)
        return 1

    text = path.read_text(encoding="utf-8")
    section = extract_section(text, "1.32.0")
    if not section:
        print("check-changelog-korean-primary: [1.32.0] section not found", file=sys.stderr)
        return 1

    # Split by blank lines (단락 단위)
    paragraphs = re.split(r"\n\s*\n", section)
    failed: list[str] = []
    for p in paragraphs:
        if not check_paragraph(p):
            failed.append(p.strip()[:80])
    if failed:
        print(f"check-changelog-korean-primary: {len(failed)} paragraph(s) fail Korean-primary rule:", file=sys.stderr)
        for f in failed:
            print(f"  - {f}", file=sys.stderr)
        return 1
    print(f"check-changelog-korean-primary: OK ({len(paragraphs)} paragraphs)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

Run:
```bash
chmod +x /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-changelog-korean-primary.py
```

- [ ] **Step 6.4: AC6 검증**

Run:
```bash
python3 /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-changelog-korean-primary.py \
  /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/CHANGELOG.md
```
Expected: `check-changelog-korean-primary: OK (N paragraphs)`, exit 0.

만약 특정 단락이 FAIL이면 해당 단락에 Hangul 추가 또는 verbatim quote 마커 사용.

- [ ] **Step 6.5: Commit**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/CHANGELOG.md \
  plugins/quality-gates/scripts/check-changelog-korean-primary.py && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
docs(quality-gates): I-C CHANGELOG [1.32.0] Korean-primary 변환

5 subsection (Breaking, Added, Changed, Removed, Migration) 본문을
Korean-primary로 변환. Technical 사실 변경 없음 — 의미 동일.

English 유지 토큰: 파일 경로, 변수명, env var, plugin/scripts 이름,
원문 인용 (백틱 코드), 식별자.

신규: scripts/check-changelog-korean-primary.py (~50 lines)
- 단락 단위 deterministic 규칙: Hangul OR verbatim quote OR 100% identifier
- 영구 보존 (향후 CHANGELOG 항목 추가 시 재검증 가능)

AC6 충족.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 전체 regression run

**Spec ref**: AC9.

- [ ] **Step 7.1: 모든 test 일괄 실행**

Run:
```bash
cd /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup && \
  set -e && \
  echo "=== test_session_start_advisor_v2 ===" && \
  bash plugins/quality-gates/tests/test_session_start_advisor_v2.sh && \
  echo "=== test_cancel_qg ===" && \
  bash plugins/quality-gates/tests/test_cancel_qg.sh && \
  echo "=== test_pre_pipeline_check ===" && \
  bash plugins/quality-gates/tests/test_pre_pipeline_check.sh && \
  echo "=== test_read_frontmatter ===" && \
  bash plugins/quality-gates/tests/test_read_frontmatter.sh && \
  echo "=== test_cancel_qg_med4 ===" && \
  bash plugins/quality-gates/tests/test_cancel_qg_med4.sh && \
  echo "=== test_check_allowed_tools_order ===" && \
  bash plugins/quality-gates/tests/test_check_allowed_tools_order.sh && \
  echo "=== test_skill_orchestration_behavior ===" && \
  bash plugins/quality-gates/tests/harness/test_skill_orchestration_behavior.sh && \
  echo "=== check-allowed-tools-order.sh ===" && \
  bash plugins/quality-gates/scripts/check-allowed-tools-order.sh && \
  echo "=== check-changelog-korean-primary.py ===" && \
  python3 plugins/quality-gates/scripts/check-changelog-korean-primary.py plugins/quality-gates/CHANGELOG.md && \
  echo "" && \
  echo "ALL REGRESSION + NEW TESTS GREEN."
```
Expected: 모든 line PASS, 마지막 `ALL REGRESSION + NEW TESTS GREEN.`.

만약 어떤 test가 FAIL → 해당 Task로 돌아가 디버깅. plan은 Task 8(version bump) 전에 모든 test green을 요구.

- [ ] **Step 7.2: AC3 추가 검증 (전수 grep)**

Run:
```bash
grep -rn "awk -F'\"'" /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/ || echo "ZERO_HITS"
```
Expected: `ZERO_HITS` 단독 출력. 만약 grep이 line을 출력하면 Task 2가 미완료.

- [ ] **Step 7.3 (no commit)**

이 task는 검증만 — commit 없음.

---

## Task 8: Final bump

**Spec ref**: AC10 (plugin.json version), AC11 (CHANGELOG [1.32.3] entry).

**Files:**
- Modify: `plugins/quality-gates/.claude-plugin/plugin.json`
- Modify: `plugins/quality-gates/CHANGELOG.md` (prepend [1.32.3] entry)

- [ ] **Step 8.1: plugin.json version bump**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/.claude-plugin/plugin.json`:

old_string:
```
  "version": "1.32.2",
```

new_string:
```
  "version": "1.32.3",
```

- [ ] **Step 8.2: CHANGELOG [1.32.3] entry prepend**

Edit `/Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/CHANGELOG.md`:

old_string:
```
## [1.32.2]
```

new_string:
```
## [1.32.3] — 2026-05-28

PR #71 (v1.32.0 → v1.32.2) merge 후 deferred된 6건의 follow-up.
모든 변경은 비기능적 polish/defense-in-depth (breaking change 없음).
3-round spec review 통과 (8 + 4 + 0 issues 모두 흡수).

### Added
- **`scripts/read-frontmatter.py`**: frontmatter `key: "value"` 파싱
  helper. escape-aware regex로 embedded `\"` / `\\` 처리. 3 call site
  (`pre-pipeline-check.sh` × 2, `cancel-qg-core.sh` × 1)의 `awk -F'"'`
  패턴 대체 (MED-3).
- **`scripts/check-allowed-tools-order.sh`**: SKILL.md `allowed-tools`
  pipeline-order 검증 linter. Canonical source = 내부 `EXPECTED_ORDER`
  배열 (single source of truth). 16 tools 5 groups (I-D).
- **`scripts/check-changelog-korean-primary.py`**: CHANGELOG `[1.32.0]`
  Korean-primary 컨벤션 단락 단위 검증 (I-C). 영구 보존 (향후 항목
  추가 시 재검증 가능).
- **`tests/test_read_frontmatter.sh`**: 5 케이스 — quoted / unquoted /
  missing / embedded-quote (val"ue) / embedded-backslash (a\b).
- **`tests/test_cancel_qg_med4.sh`**: MED-4 검증. mv backup + cp stub +
  `trap '...' EXIT` 패턴으로 fixture stub 통한 실패 경로 검증 (3 assertion:
  stub 메시지 prefix / exit code 1 라인 / sed 0건).
- **`tests/test_check_allowed_tools_order.sh`**: linter 4 시나리오 —
  canonical PASS / within-group swap FAIL / cross-group move FAIL /
  unknown tool FAIL.
- **`tests/fixtures/qg-worktree-fail-stub.sh`**: MED-4 영구 fixture.
  qg-worktree.sh 실패 simulator (exit 1 + stderr 메시지).

### Changed
- **`cancel-qg-core.sh`**:
  - MED-1: qg-worktree.sh 부재/비실행 메시지 self-actionable화. MISSING
    vs EXISTS-but-not-executable 구별, 사용자 직접 실행 명령
    (`git worktree remove --force "<path>"`) 명시.
  - MED-4: sed pipe 제거. `cmd | sed` 대신 stdout/stderr 병합 capture +
    수동 prefix. `if/else` 형태로 `set -e` 안전하게 exit code 캡처.
    qg-worktree.sh 출력 계약(병합 스트림 prefix-emit) 보존.
  - MED-3 transition: `awk -F'"'` 호출부를 `read-frontmatter.py` 호출로
    교체. `SCRIPT_DIR` 변수를 file top으로 끌어올려 lowercase `script_dir`
    정의와 통일.
- **`pre-pipeline-check.sh`**: MED-3 transition 2 site
  (`branch` / `session_id` 파싱). `tr -d '[:space:]'` 제거 (helper가
  `.strip()` 내부 처리). `SCRIPT_DIR` 변수 file top에 추가.
- **`skills/quality-pipeline/SKILL.md`**: `allowed-tools` frontmatter
  pipeline-order 재정렬 (I-D). 5 group 경계를 YAML comment로 inline 문서화.
- **`CHANGELOG.md` `[1.32.0]` body**: English prose → Korean-primary 변환
  (I-C). Technical 사실 변경 없음.

### Fixed
- **`tests/test_pre_pipeline_check.sh`**: MED-2 SID guard 4 boundary
  케이스 추가 — empty / too-short (7 char) / invalid-char (`abc/def123`) /
  valid (15 char + sandbox `git init` + fresh state로 isolation).

### Acceptance Criteria
- AC1 (MED-1): cancel-qg-core.sh stderr 메시지 검증
- AC2 (MED-2): 4 SID boundary 케이스 PASS
- AC3 (MED-3 transition): `awk -F'"'` 0 hits, `SCRIPT_DIR` 정의 확인
- AC4 (MED-3 unit): 5 helper 케이스 PASS
- AC5 (MED-4): sed 0건, exit code 라인 검증
- AC6 (I-C): `check-changelog-korean-primary.py` PASS
- AC7 (I-D ordering): linter exit 0
- AC8 (I-D linter unit): 4 scenarios PASS
- AC9 (regression): 기존 testsuite + 신규 7 test 전체 PASS
- AC10: `plugin.json.version` == `"1.32.3"`
- AC11: 이 CHANGELOG entry 존재

## [1.32.2]
```

- [ ] **Step 8.3: AC10 + AC11 검증**

Run:
```bash
grep -c '"version": "1.32.3"' /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/.claude-plugin/plugin.json && \
  head -5 /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/CHANGELOG.md
```
Expected: `1` (count 한 줄) + CHANGELOG 첫 줄이 `## [1.32.3] — 2026-05-28`.

- [ ] **Step 8.4: Korean-primary 재검증 (새 [1.32.3] entry 포함)**

Run:
```bash
python3 /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/scripts/check-changelog-korean-primary.py \
  /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup/plugins/quality-gates/CHANGELOG.md
```
(이 verifier는 `[1.32.0]`만 검사하므로 새 `[1.32.3]`은 영향 없음. 그러나 향후 사용 가능 — Korean-primary가 유지되는지 spot check.)

Expected: exit 0.

- [ ] **Step 8.5: Final commit (PR-ready)**

Run:
```bash
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup add \
  plugins/quality-gates/.claude-plugin/plugin.json \
  plugins/quality-gates/CHANGELOG.md && \
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup commit -m "$(cat <<'EOF'
chore(quality-gates): v1.32.2 → v1.32.3 + CHANGELOG [1.32.3]

PR #71 deferred 6건 (MED-1..4, I-C, I-D) follow-up 마무리.
Spec: docs/superpowers/specs/2026-05-28-qg-v1322-followup-design.md
(v1.2.0, 3-round review approved).

신규 파일 7 (helper 3 + test 3 + fixture 1).
수정 파일 6 (cancel-qg-core, pre-pipeline-check, SKILL.md,
test_pre_pipeline_check, CHANGELOG, plugin.json).

AC1-AC11 모두 충족. 기존 testsuite + 신규 7 test 전체 PASS.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 8.6: PR 준비 — push + gh pr create는 사용자 승인 후**

Plan은 여기까지. PR 생성은 사용자 확인 후 별도 단계:

```bash
# 사용자 승인 시:
git -C /Users/jeonghokim/Downloads/devbrew/.claude/worktrees/feature-qg-v1322-followup push -u origin feature/qg-v1322-followup
gh pr create --title "feat(quality-gates): v1.32.3 — PR #71 deferred 6건 follow-up" --body "..."
```

---

## Self-Review

이 plan을 spec과 대조 — placeholder, type consistency, coverage 검사. 인라인 실행이므로 별도 subagent 불필요.

**1. Spec coverage 점검**:

| Spec § | AC | Task | 커버됨? |
|---|---|---|---|
| §4.1 MED-1 | AC1 | Task 3 (Step 3.2, 3.6) | ✓ |
| §4.2 MED-2 | AC2 | Task 4 (4 boundary cases) | ✓ |
| §4.3 MED-3 helper | AC3, AC4 | Task 1, Task 2 | ✓ |
| §4.4 MED-4 | AC5 | Task 3 (Step 3.1, 3.4) | ✓ |
| §4.5 I-C | AC6 | Task 6 | ✓ |
| §4.6 I-D | AC7, AC8 | Task 5 | ✓ |
| Regression | AC9 | Task 7 (전수) | ✓ |
| Version bump | AC10, AC11 | Task 8 | ✓ |

**2. Deferred-to-plan 4건 점검**:

| Spec §0.1 deferred | Plan 처리 |
|---|---|
| 각 fix commit message 본문 정확 문구 | 각 Task의 Step `-m "$(cat <<'EOF' ... EOF)"` 형식으로 명시 |
| qg-worktree-fail-stub.sh chmod +x 배치 | Task 3 Step 3.3 (생성 직후) |
| CHANGELOG [1.32.3] 항목별 카피 | Task 8 Step 8.2 (verbatim 포함) |
| spec_version bump 여부 | Plan 외 (advisory, 미적용) |

**3. Placeholder scan**: TBD / TODO / "implement later" / "appropriate" / "edge cases" 패턴 0건. 모든 code block은 실제 실행 가능 content.

**4. Type / 이름 consistency**:
- helper 이름 `read-frontmatter.py`: Task 1 (생성), Task 2 (호출), Task 3 (호출 — 마찬가지) → 일관.
- `SCRIPT_DIR` 변수: pre-pipeline-check.sh + cancel-qg-core.sh 양쪽에 동일 이름 정의 → 일관.
- fixture 경로 `tests/fixtures/qg-worktree-fail-stub.sh`: Task 3 Step 3.3 (생성), Task 3 Step 3.4 (참조) → 일관.
- AC 식별자 AC1-AC11: Task ↔ Spec ↔ commit message 모두 동일 번호 → 일관.

**5. 작업 순서 의존성**:
- Task 1 (helper) → Task 2 (호출 사이트 전환) — 정합 (helper 없으면 Task 2 test 실패)
- Task 2 (cancel-qg-core SCRIPT_DIR 추가) → Task 3 (MED-1/MED-4 동일 파일 수정) — 정합
- Task 3 → Task 7 (regression) → Task 8 (version bump) — 정합 (모든 test green 후 bump)

Self-review PASS. plan executable.
